# Active Deprecations Register

No active deprecations are currently registered.

Current framework baseline: FastAPI application startup now uses lifespan handlers, so there is no active `on_event("startup")` deprecation path left in production code.

When adding a deprecation, append a row to the table below.

| ID | Component | Replacement | Stage | Owner | Announced | Warn From | Removal Target | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
