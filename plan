Project Plan: Open Work Only
Last updated: 2026-02-27
Owner: Engineering

================================================
0) Scope
================================================

This file contains only active work.
Completed phases and detailed historical logs were removed for clarity.
Full implementation history remains in git.

Primary goals now:
- Close remaining correctness gaps found in the 2026-02-25 code review.
- Improve code clarity and organization with an explicit OOP refactor track.

Out of scope:
- New product features unrelated to the open findings below.
- Visual redesign work.

================================================
1) Priority Order
================================================

P0 (fix first):
41. Enforce tenant scope in assign-companies document endpoints.

P1 (high priority):
42. Wire visibility filter end-to-end in documents list page.
43. Align delete action permission UX with backend authorization.
44. Make document delete + audit logging atomic.

P2 (near-term architecture and organization):
45. Standardize backend service architecture around OOP service objects.
46. Split oversized backend modules into cohesive OOP components.
47. Decompose DocumentsPage into feature components + controller hooks.
48. Refactor collaboration server bootstrap into explicit classes/services.
49. Simplify frontend API client composition and module boundaries.
50. Introduce explicit access-policy classes for permission/tenant checks.
51. Add repository classes for core aggregates and reduce direct route-level queries.
52. Add Unit-of-Work transaction abstraction for multi-step write flows.
53. Introduce domain events for decoupled side effects.
54. Introduce value objects for core domain primitives.
55. Model document/review lifecycle rules as workflow state machine classes.
56. Apply CQRS-lite command/query handlers for core domains.

P3 (deeper OOP hardening):
57. Refactor notification/email pipeline into channel + template objects.
58. Encapsulate token/auth lifecycle into dedicated auth context services.
59. Create FastAPI app factory + router registry object model.
60. Refactor attachment conversion into strategy/pipeline classes.
61. Extract collaboration backend managers into service objects.
62. Introduce frontend feature controller/view-model objects across complex screens.
63. Add typed API DTO mapper layer between transport and UI/domain.
64. Introduce domain error hierarchy + centralized error mapping.
65. Add backend/frontend test builders and fixture factories.
66. Define bounded-context modules with explicit context-level APIs.
67. Introduce aggregate roots with invariant methods.
68. Adopt ports-and-adapters boundaries for infrastructure dependencies.
69. Add specification objects for reusable authorization/filter query logic.
70. Introduce command handler pipeline (validate/authorize/execute/publish).
71. Expand query handlers/read models for analytics/search/portal reads.

P4 (strategic architecture and reliability):
72. Implement outbox pattern for reliable domain-event delivery.
73. Introduce saga/process-manager orchestration for multi-step workflows.
74. Add typed `Result`/error algebra in application/domain layers.
75. Add composition root / dependency container for service wiring.
76. Introduce frontend feature modules with use-case services.
77. Model frontend workflows with explicit state machines.
78. Adopt contract-first generated API clients + DTO contracts.
79. Add backend-frontend-collab contract tests for payload/token compatibility.
80. Expand object-mother/scenario fixture patterns across all domains.
81. Add architecture fitness tests (import/layer boundary enforcement in CI).
82. Introduce class-based controller handlers per bounded context.
83. Add domain factories for complex aggregate creation workflows.
84. Introduce capability objects for authorization decisions.
85. Add plugin registry system for extension points (converters/exporters/channels).
86. Introduce anti-corruption layers for external integrations/contracts.
87. Enforce immutable command/query DTOs across application boundaries.
88. Introduce explicit workflow-stage state objects for lifecycle flows.
89. Add use-case metrics/tracing objects for command/query observability.

P5 (long-horizon architecture):
90. Evaluate and pilot selective event sourcing for high-complexity domains.
91. Establish Architecture Decision Records (ADR) workflow for major refactors.
92. Define deprecation lifecycle policy for APIs/modules/contracts.
93. Split codebase into enforceable internal packages/layers (`domain`, `application`, `infrastructure`, `web`).
94. Add refactor-budget CI gates (size/complexity deltas for touched files).
95. Add fault-injection resilience tests for external adapters and integrations.
96. Publish migration playbooks/checklists for each refactor wave.
97. Define internal contract versioning policy for backend/frontend/collab payloads.
98. Define use-case performance SLOs and monitor command/query p95/error budgets.
99. Introduce feature-toggle framework for safe architectural rollouts.
100. Enforce "no direct DB access in routes" rule with lint checks/codemod support.

P6 (advanced architecture controls):
102. Introduce centralized Policy Decision Point (PDP) authorization service.
111. Define explicit use-case domain service interfaces (non-CRUD).
112. Add invariant specification objects reusable at runtime and in tests.
114. Add policy-explanation objects for authorization decisions.
109. Enforce frontend feature-level dependency rules.
101. Add idempotency-key support for write endpoints and side-effecting operations.
107. Add optimistic concurrency controls (`version`/`etag`) for mutable resources.
113. Introduce command bus + query bus middleware pipelines.
116. Add formal executable workflow models + model-based tests for high-risk flows.
117. Add consumer-driven contract testing (frontend/backed provider suite).
103. Add tenant-isolation attack harness for endpoint-level security regression tests.
121. Create architecture debt register with ownership/risk tracking.

P7 (platform and governance maturity):
104. Publish data-ownership map per bounded context (single write-owner policy).
115. Create context-map artifacts describing domain upstream/downstream dependencies.
122. Add refactor scaffolding generators/templates for consistent patterns.
108. Apply strangler wrappers for legacy-heavy modules.
106. Build unified async job framework (retry/DLQ/poison handling/visibility).
105. Implement projection/caching architecture with event-driven invalidation.
110. Introduce Backend-for-Frontend (BFF) layer for complex UI orchestration.
118. Build migration safety framework (dry-run/preflight/rollback probes/post-assertions).
119. Add SLO burn-rate alerts tied to use-cases.
120. Add chaos testing on adapter boundaries in staging pipeline.

Execution waves:
- Wave D (correctness fixes): 41, 42, 43, 44.
- Wave E (governance and domain mapping prerequisites): 91, 92, 96, 97, 104, 115, 121.
- Wave F (structural boundaries and enforcement baseline): 66, 93, 68, 81, 109, 100, 94.
- Wave G (backend application core foundations): 45, 50, 51, 52, 111, 74, 64, 75, 102, 114.
- Wave H (domain model hardening): 54, 67, 83, 112, 69, 84, 87.
- Wave I (workflow and use-case orchestration): 55, 88, 70, 56, 71, 113, 116.
- Wave J (write-path reliability and rollout safety): 53, 72, 73, 101, 107, 106, 105, 99, 108, 118.
- Wave K (service and integration refactors): 59, 82, 48, 61, 58, 57, 60, 46, 85, 86.
- Wave L (frontend architecture and contract alignment): 49, 63, 47, 62, 76, 77, 78, 110, 117, 79.
- Wave M (testability and scaffolding acceleration): 65, 80, 122.
- Wave N (security and resilience validation): 103, 95, 120.
- Wave O (observability and long-horizon evolution): 89, 98, 119, 90.
- Wave playbooks/checklists: docs/migrations/README.md

================================================
2) Active Tasks
================================================

Task 41: Tenant-scoped assign-companies endpoints (P0)

Status:
- Completed (2026-02-27).

Why:
- Current assign/remove/list company assignment handlers fetch document by ID without tenant scoping.
- Role permission checks alone are not enough for tenant isolation.

Primary files:
- backend/app/api/management/documents.py
- backend/app/services/document_service.py
- backend/app/dependencies/permissions.py
- backend/tests/test_documents.py

Implementation plan:
- Route all company-assignment read/write operations through tenant-aware document access.
- Reuse a single access path for "document exists + user can access tenant" checks.
- Add explicit negative tests for cross-tenant assignment attempts.

Acceptance criteria:
- Non-system-admin users cannot assign/unassign/list companies for cross-tenant documents.
- API returns not-found/forbidden consistently with tenant access policy.
- Regression tests cover same-tenant success and cross-tenant denial.

------------------------------------------------

Task 42: Visibility filter wiring on Documents page (P1)

Status:
- Completed (2026-02-27).

Why:
- UI has visibility filter controls, but query payload does not include visibility.
- This makes filtering appear functional while returning unfiltered data.

Primary files:
- frontend/src/pages/DocumentsPage.tsx
- frontend/src/lib/api/documentsApi.ts
- backend/app/api/management/documents.py

Implementation plan:
- Add `visibility` to query key and API request payload.
- Ensure backend list endpoint accepts and applies visibility filter.
- Add/update frontend tests for filter behavior.

Acceptance criteria:
- Selecting visibility updates request params and list results.
- Query cache keys vary correctly by visibility.
- Existing status/search filters continue to work.

------------------------------------------------

Task 43: Delete permission UX parity (P1)

Status:
- Completed (2026-02-27).

Why:
- Backend requires manager or above to delete.
- Documents page currently shows delete action to broader roles.

Primary files:
- frontend/src/pages/DocumentsPage.tsx
- frontend/src/lib/auth.tsx
- backend/app/api/management/documents.py

Implementation plan:
- Restrict delete button rendering and action path to authorized roles only.
- Keep backend authorization unchanged as source of truth.
- Add UI-level permission tests.

Acceptance criteria:
- Unauthorized roles do not see or trigger delete action in UI.
- Authorized roles keep existing delete flow.
- No role-based regression in other document actions.

------------------------------------------------

Task 44: Atomic document delete transaction (P1)

Status:
- Completed (2026-02-27).

Why:
- Delete flow currently commits audit and delete in separate transactions.
- Partial failure can leave inconsistent audit/state outcomes.

Primary files:
- backend/app/services/document_service.py
- backend/tests/test_documents.py

Implementation plan:
- Wrap audit creation and document deletion in a single transaction boundary.
- Ensure rollback-safe behavior if delete fails.
- Add regression test for failure scenario consistency.

Acceptance criteria:
- Delete + audit either both commit or both rollback.
- No duplicate/partial commits across retries.

------------------------------------------------

Task 45: Backend OOP service standardization (P2)

Status:
- Completed (2026-02-27).

Why:
- Service layer mixes instance-style services with large static utility classes.
- Tenant-aware base service exists but is not consistently used.

Primary files:
- backend/app/services/base_service.py
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- backend/app/services/comment_service.py
- backend/app/services/auth_service.py

Implementation plan:
- Define standard service shape: constructor-injected dependencies + instance methods.
- Introduce domain services/repositories/policies for shared access and permission checks.
- Incrementally migrate static-heavy services without changing external API behavior.

Acceptance criteria:
- Core services follow one consistent OOP style.
- Direct route-level DB access is reduced in favor of service methods.
- Shared tenant/permission logic is centralized and reused.

------------------------------------------------

Task 46: Split oversized backend modules (P2)

Status:
- Completed (2026-02-28).

Why:
- Several modules are too large and combine unrelated responsibilities.

Primary files:
- backend/app/utils/document_converter.py
- backend/app/services/analytics_service.py
- backend/app/db.py

Implementation plan:
- Split converter into parser/heuristics/renderer modules with clear interfaces.
- Split analytics into query components by dashboard section.
- Move DB lightweight migration routines into dedicated migration helper modules.

Acceptance criteria:
- Module sizes and responsibilities are reduced and clearer.
- Existing behavior and tests remain stable.
- New internal interfaces are documented in code comments/docstrings.

------------------------------------------------

Task 47: Decompose DocumentsPage into components/hooks (P2)

Status:
- Completed (2026-02-28).

Why:
- DocumentsPage contains list screen + modal forms + business flow logic in one file.
- This makes reasoning, testing, and role-based behavior harder.

Primary files:
- frontend/src/pages/DocumentsPage.tsx
- frontend/src/hooks/useDocumentQueries.ts
- frontend/src/components/*

Implementation plan:
- Extract create/upload/quick-start modals into dedicated components.
- Add controller hooks (`useDocumentsPageController`, `useCreateDocumentFlow`, `useUploadDocumentFlow`).
- Keep page component focused on composition and layout.

Acceptance criteria:
- DocumentsPage is significantly smaller and primarily declarative.
- Business logic is testable via hooks.
- No behavior regressions in create/upload/delete/filters/pagination.

------------------------------------------------

Task 48: OOP refactor for collab-server bootstrap (P2)

Status:
- Completed (2026-02-27).

Why:
- Server startup file currently owns config, connection state, auth hooks, persistence hooks, and health server setup.

Primary files:
- collab-server/src/index.ts
- collab-server/src/persistence.ts
- collab-server/src/auth.ts

Implementation plan:
- Introduce `CollabServerApp`, `ConnectionRegistry`, and `HealthServer` classes.
- Keep hook handlers small and delegate behavior to services.
- Preserve public runtime behavior and environment variables.

Acceptance criteria:
- `index.ts` becomes a thin bootstrap entrypoint.
- Connection/auth/persistence concerns are separated into dedicated classes.
- Existing collab-server tests continue passing with updated unit coverage as needed.

------------------------------------------------

Task 49: Frontend API client composition cleanup (P2)

Status:
- Completed (2026-02-28).

Why:
- Deep mixin composition makes the API surface hard to navigate and evolve.

Primary files:
- frontend/src/lib/api.ts
- frontend/src/lib/api/httpClient.ts
- frontend/src/lib/api/*.ts

Implementation plan:
- Replace deep nested mixin chain with clearer module composition/factory structure.
- Keep existing method signatures stable for callers.
- Add small architecture notes in file headers.

Acceptance criteria:
- API client composition is easier to follow and extend.
- No consumer changes required outside API module boundaries.
- Typecheck/tests remain clean.

------------------------------------------------

Task 50: Access-policy object model (P2)

Status:
- Completed (2026-02-27).

Why:
- Permission and tenant checks are duplicated across services, dependencies, and routes.

Primary files:
- backend/app/services/permissions.py
- backend/app/dependencies/permissions.py
- backend/app/services/collaboration_service.py
- backend/app/api/management/*

Implementation plan:
- Add policy classes (`DocumentAccessPolicy`, `ReviewPolicy`, `InvitationPolicy`).
- Inject policy objects into services instead of ad-hoc helper calls.
- Keep compatibility wrappers during migration.

Acceptance criteria:
- Access decisions for each domain are centralized and testable.
- Route/service-level permission duplication is reduced.

------------------------------------------------

Task 51: Repository layer for core aggregates (P2)

Status:
- Completed (2026-02-27).

Why:
- Direct ORM query usage is scattered across routers and services.

Primary files:
- backend/app/services/*
- backend/app/api/management/*
- backend/app/api/portal/*
- backend/app/repositories/* (new)

Implementation plan:
- Add repositories for `Document`, `Version`, `User`, `Comment`, `Invitation`.
- Move common query patterns (tenant scope, eager loading, filtering) into repositories.
- Migrate targeted modules incrementally.

Acceptance criteria:
- Targeted routes stop performing direct aggregate fetch queries.
- Query behavior is reusable and consistent across endpoints.

------------------------------------------------

Task 52: Unit-of-Work transaction abstraction (P2)

Status:
- Completed (2026-02-27).

Why:
- Multi-step writes still risk partial commits when logic is distributed.

Primary files:
- backend/app/db.py
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- backend/app/services/comment_service.py
- backend/app/services/uow.py (new)

Implementation plan:
- Introduce a Unit-of-Work helper to control commit/rollback boundaries.
- Migrate high-risk write flows first (create/update/delete/publish/upload chains).

Acceptance criteria:
- Multi-step write use-cases are wrapped in explicit transaction scopes.
- Rollback semantics are deterministic and covered by tests.

------------------------------------------------

Task 53: Domain events for side effects (P2)

Status:
- Completed (2026-02-27).

Why:
- Core write flows currently trigger notifications/email inline.

Primary files:
- backend/app/services/version_service.py
- backend/app/services/comment_service.py
- backend/app/services/email_service.py
- backend/app/domain/events/* (new)

Implementation plan:
- Emit domain events (`DocumentPublished`, `CommentCreated`, etc.).
- Dispatch side effects via event handlers.
- Keep in-process dispatcher initially; design for outbox extension.

Acceptance criteria:
- Core domain operations do not directly orchestrate side effects.
- Event handlers own notification/email dispatch.

------------------------------------------------

Task 54: Value objects for domain primitives (P2)

Status:
- Completed (2026-02-27).

Why:
- Semantic versioning, document numbering, and topic normalization are currently string-heavy utility logic.

Primary files:
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- backend/app/utils/topic_normalization.py
- backend/app/domain/value_objects/* (new)

Implementation plan:
- Add `DocumentNumber`, `SemanticVersion`, `TopicSlug` value objects.
- Replace ad-hoc parse/format logic with value object methods.

Acceptance criteria:
- Domain primitive validation/parsing lives in dedicated value object classes.
- Duplicate parsing logic is removed from services.

------------------------------------------------

Task 55: Workflow state-machine classes (P2)

Status:
- Completed (2026-02-27).

Why:
- Lifecycle transitions are enforced by conditionals spread across services.

Primary files:
- backend/app/models/__init__.py
- backend/app/services/version_service.py
- backend/app/api/management/reviews.py
- backend/app/domain/workflows/* (new)

Implementation plan:
- Add workflow classes for document and review status transitions.
- Expose explicit transition methods (`can_transition`, `transition`).

Acceptance criteria:
- Invalid transitions are blocked by centralized workflow logic.
- Transition rules are testable without HTTP-layer coupling.

------------------------------------------------

Task 56: CQRS-lite command/query handlers (P2)

Status:
- Completed (2026-02-27).

Why:
- Services currently mix read aggregation and write orchestration responsibilities.

Primary files:
- backend/app/services/analytics_service.py
- backend/app/services/document_service.py
- backend/app/api/management/analytics.py
- backend/app/api/management/documents.py
- backend/app/application/commands/* (new)
- backend/app/application/queries/* (new)

Implementation plan:
- Introduce command handlers for write use-cases and query handlers for read models.
- Start with analytics and document management domains.

Acceptance criteria:
- Read models and write workflows are separated by handler type.
- Modules become easier to reason about and test independently.

------------------------------------------------

Task 57: Notification/email channel-template objects (P3)

Status:
- Completed (2026-02-27).

Why:
- Email message rendering, subject creation, and transport are tightly coupled.

Primary files:
- backend/app/services/email_service.py
- backend/app/services/version_service.py
- backend/app/services/comment_service.py
- backend/app/notifications/* (new)

Implementation plan:
- Split into template objects, message objects, and delivery channels.
- Add a notification dispatcher facade used by domain-event handlers.

Acceptance criteria:
- Message templates and channel delivery are independently testable.
- Adding channels (e.g., in-app/webhook) does not require touching domain logic.

------------------------------------------------

Task 58: Auth context services (P3)

Status:
- Completed (2026-02-27).

Why:
- Access, refresh, and collaboration token logic is split across multiple modules.

Primary files:
- backend/app/services/auth_service.py
- backend/app/security.py
- backend/app/services/collaboration_service.py
- collab-server/src/auth.ts

Implementation plan:
- Introduce `TokenService`, `RefreshTokenService`, and `CollaborationAuthService`.
- Centralize token payload contracts and validation rules.

Acceptance criteria:
- Token issuance/validation responsibilities are cleanly separated.
- Token contract changes require edits in a single bounded area.

------------------------------------------------

Task 59: FastAPI app factory and router registry (P3)

Status:
- Completed (2026-02-27).

Why:
- Application bootstrap and router wiring are concentrated in one large module.

Primary files:
- backend/app/main.py
- backend/app/app_factory.py (new)
- backend/app/router_registry.py (new)

Implementation plan:
- Move app creation to `create_app()` with configurable wiring.
- Register routers through a registry object and explicit modules.

Acceptance criteria:
- `main.py` becomes a thin entrypoint.
- Middleware/router startup wiring is modular and testable.

------------------------------------------------

Task 60: Attachment conversion strategy pipeline (P3)

Status:
- Completed (2026-02-27).

Why:
- Conversion logic currently mixes format detection, extraction, and rendering in giant utilities.

Primary files:
- backend/app/utils/document_converter.py
- backend/app/services/attachment_service/reader_view.py
- backend/app/services/attachment_service/preview_pdf.py
- backend/app/conversion/* (new)

Implementation plan:
- Implement converter strategies (`PdfConverter`, `WordConverter`, `TextConverter`).
- Add pipeline coordinator for fallback and artifact assembly.

Acceptance criteria:
- Conversion behavior is expressed via strategy classes and shared interfaces.
- New formats can be added without editing a monolithic converter file.

------------------------------------------------

Task 61: Collaboration backend managers (P3)

Status:
- Completed (2026-02-27).

Why:
- Collaboration endpoints and service methods blend state, permissions, and session concerns.

Primary files:
- backend/app/api/management/collaboration/*
- backend/app/services/collaboration_service.py
- backend/app/services/snapshot_service.py
- backend/app/collaboration/* (new)

Implementation plan:
- Add `CollabStateManager`, `SessionManager`, `SnapshotManager`.
- Keep API handlers thin and delegate workflow logic.

Acceptance criteria:
- Collaboration domain responsibilities are split into focused manager classes.
- Endpoint handlers mostly orchestrate input/output only.

------------------------------------------------

Task 62: Frontend feature controllers/view-models (P3)

Status:
- Completed (2026-02-28).

Why:
- Large page components still mix UI rendering and orchestration logic.

Primary files:
- frontend/src/pages/DocumentsPage.tsx
- frontend/src/pages/document-detail/*
- frontend/src/lib/useCollaboration.ts
- frontend/src/features/* (new)

Implementation plan:
- Create per-feature controllers/view-model hooks.
- Move business decisions from page components to feature-level controller modules.

Acceptance criteria:
- Complex pages are composition-oriented and significantly smaller.
- Feature logic is unit-testable without rendering full pages.

------------------------------------------------

Task 63: Typed DTO mapper layer (P3)

Status:
- Completed (2026-02-28).

Why:
- UI and services frequently consume transport payloads directly.

Primary files:
- frontend/src/lib/api/*.ts
- frontend/src/types/index.ts
- backend/app/schemas/*
- backend/app/domain/dto_mappers/* (new)

Implementation plan:
- Add explicit transport-to-domain mappers and reverse mappers where needed.
- Standardize response shape adaptation in one layer.

Acceptance criteria:
- API transport details are isolated from domain/UI models.
- Schema evolution requires fewer broad changes.

------------------------------------------------

Task 64: Domain error hierarchy and result objects (P3)

Status:
- Completed (2026-02-27).

Why:
- Service layer relies heavily on `HTTPException`, coupling domain logic to transport layer.

Primary files:
- backend/app/services/*
- backend/app/main.py
- backend/app/errors.py (new)

Implementation plan:
- Add domain exceptions (`DomainError`, `PermissionDeniedError`, etc.).
- Map domain exceptions to HTTP responses in centralized handlers.

Acceptance criteria:
- Domain services avoid direct HTTP error construction where practical.
- Error-to-response mapping is centralized and consistent.

------------------------------------------------

Task 65: Test builders/factories for domain setup (P3)

Status:
- Not started.

Why:
- Tests repeat heavy setup logic that obscures behavior under test.

Primary files:
- backend/tests/conftest.py
- backend/tests/factories/* (new)
- frontend/src/test/*
- collab-server/src/__tests__/*

Implementation plan:
- Add object builders/factories for common entities and workflows.
- Use scenario-style fixtures for multi-step flows.

Acceptance criteria:
- Test setup boilerplate is reduced in targeted suites.
- Test intent is clearer and easier to maintain.

------------------------------------------------

Task 66: Bounded-context module boundaries (P3)

Status:
- Completed (2026-02-27).

Why:
- Domain concerns are spread across broad `services/api` modules with implicit coupling.

Primary files:
- backend/app/api/*
- backend/app/services/*
- backend/app/models/__init__.py
- frontend/src/pages/*
- frontend/src/lib/*

Implementation plan:
- Define explicit context boundaries (documents, reviews, collaboration, tenants, notifications).
- Add context-level public APIs/facades for cross-context calls.
- Restrict cross-context imports to public interfaces.

Acceptance criteria:
- Context boundaries are explicit and documented in module structure.
- Cross-context dependencies go through stable interfaces.

------------------------------------------------

Task 67: Aggregate roots and invariants (P3)

Status:
- Completed (2026-02-27).

Why:
- Core invariants are enforced through scattered service conditionals instead of domain objects.

Primary files:
- backend/app/models/__init__.py
- backend/app/domain/aggregates/* (new)
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- backend/app/services/comment_service.py

Implementation plan:
- Introduce aggregate root classes (`DocumentAggregate`, `ReviewAggregate`, `InvitationAggregate`).
- Move invariant checks and state transitions into aggregate methods.
- Keep persistence mapping in repositories/services.

Acceptance criteria:
- Invariant logic is centralized in aggregate methods.
- Service layer orchestration code is reduced.

------------------------------------------------

Task 68: Ports and adapters architecture (P3)

Status:
- Completed (2026-02-27).

Why:
- Domain/application logic is still tightly coupled to concrete infrastructure libraries.

Primary files:
- backend/app/services/email_service.py
- backend/app/services/storage_service.py
- backend/app/services/collaboration_service.py
- collab-server/src/persistence.ts
- backend/app/ports/* (new)
- backend/app/adapters/* (new)

Implementation plan:
- Define ports for email, storage, collaboration state, and persistence interactions.
- Implement concrete adapters for SMTP/S3/local/hocuspocus integrations.
- Inject ports into services via constructors/factories.

Acceptance criteria:
- Domain/application services depend on interfaces, not concrete adapters.
- Infrastructure replacements require minimal domain-layer changes.

------------------------------------------------

Task 69: Specification pattern for reusable queries (P3)

Status:
- Completed (2026-02-27).

Why:
- Tenant, visibility, and role-based filters are duplicated across queries.

Primary files:
- backend/app/repositories/* (new)
- backend/app/services/permissions.py
- backend/app/api/management/search.py
- backend/app/api/portal/documents.py

Implementation plan:
- Add specification objects (`TenantScopeSpec`, `VisibilitySpec`, `RoleAccessSpec`, `DateRangeSpec`).
- Compose specifications in repositories/query handlers.
- Replace repeated inline filter blocks in target endpoints.

Acceptance criteria:
- Core filter logic is reusable/composable and tested once.
- Query duplication drops in targeted modules.

------------------------------------------------

Task 70: Command handler pipeline (P3)

Status:
- Completed (2026-02-27).

Why:
- Write use-cases repeat the same orchestration steps in inconsistent order.

Primary files:
- backend/app/application/commands/* (new)
- backend/app/application/pipeline/* (new)
- backend/app/api/management/documents.py
- backend/app/api/management/reviews.py

Implementation plan:
- Create command pipeline stages: validate -> authorize -> execute -> publish events.
- Wrap core write use-cases in command handlers.
- Keep HTTP handlers as thin adapters to commands.

Acceptance criteria:
- Command execution flow is standardized and traceable.
- Write endpoints share common pipeline behavior.

------------------------------------------------

Task 71: Query handlers and read models (P3)

Status:
- Completed (2026-02-27).

Why:
- Read complexity (analytics/search/portal) is mixed into service and route layers.

Primary files:
- backend/app/application/queries/* (new)
- backend/app/services/analytics_service.py
- backend/app/api/management/analytics.py
- backend/app/api/management/search.py
- backend/app/api/portal/documents.py

Implementation plan:
- Move high-complexity reads into dedicated query handlers/read-model classes.
- Keep write-oriented services focused on command-side behavior.

Acceptance criteria:
- Read logic is separated from write orchestration in target domains.
- Query handlers are independently testable.

------------------------------------------------

Task 72: Outbox pattern for domain events (P4)

Status:
- Completed (2026-02-27).

Why:
- Side effects can be lost or duplicated when transaction boundaries and dispatch timing diverge.

Primary files:
- backend/app/models/__init__.py
- backend/alembic/versions/* (new)
- backend/app/workers/*
- backend/app/domain/events/*

Implementation plan:
- Add outbox table and dispatcher worker.
- Persist events in the same transaction as domain state changes.
- Implement idempotent event processing and retries.

Acceptance criteria:
- Event delivery is reliable across process failures/restarts.
- Side-effect handlers run from persisted outbox records.

------------------------------------------------

Task 73: Saga/process-manager workflows (P4)

Status:
- Completed (2026-02-27).

Why:
- Multi-step workflows currently rely on local try/except compensation logic.

Primary files:
- backend/app/api/management/documents.py
- backend/app/services/conversion_jobs.py
- backend/app/services/attachment_service/*
- backend/app/application/process_managers/* (new)

Implementation plan:
- Introduce process managers for upload/conversion/release-notes workflows.
- Define explicit steps, compensation actions, and failure states.

Acceptance criteria:
- Long-running multi-step flows have explicit orchestration state.
- Compensation behavior is deterministic and observable.

------------------------------------------------

Task 74: Typed Result/error algebra in domain/application layers (P4)

Status:
- Completed (2026-02-27).

Why:
- Expected business failures are still often represented via exceptions.

Primary files:
- backend/app/domain/result.py (new)
- backend/app/services/*
- backend/app/application/commands/*
- backend/app/application/queries/*

Implementation plan:
- Introduce `Result[T, E]` for expected domain/application outcomes.
- Use exception mapping only at transport boundaries for unexpected failures.
- Align with Task 64 error taxonomy.

Acceptance criteria:
- Selected command/query handlers return typed results for expected failures.
- Error handling paths are explicit and typed in core flows.

------------------------------------------------

Task 75: Composition root / dependency container (P4)

Status:
- Completed (2026-02-27).

Why:
- Service construction/wiring remains distributed across routes/modules.

Primary files:
- backend/app/main.py
- backend/app/app_factory.py
- backend/app/container.py (new)
- collab-server/src/index.ts

Implementation plan:
- Add central composition root for constructing repositories/services/policies.
- Route/dependency layers resolve dependencies through a shared container.

Acceptance criteria:
- Dependency wiring is centralized and auditable.
- Constructor injection becomes consistent across major services.

------------------------------------------------

Task 76: Frontend feature modules and use-case services (P4)

Status:
- Completed (2026-02-28).

Why:
- Business orchestration is still mostly colocated with page-level UI code.

Primary files:
- frontend/src/features/* (new)
- frontend/src/pages/*
- frontend/src/lib/api/*

Implementation plan:
- Organize frontend by feature modules with explicit use-case service layer.
- Keep page components focused on layout/composition.
- Share use-case logic between pages/components where relevant.

Acceptance criteria:
- Complex features expose reusable use-case services.
- Page components in targeted features become thinner and easier to test.

------------------------------------------------

Task 77: Frontend workflow state machines (P4)

Status:
- Completed (2026-02-28).

Why:
- Multi-state flows rely on many boolean flags that are hard to reason about.

Primary files:
- frontend/src/lib/useCollaboration.ts
- frontend/src/pages/document-detail/hooks/*
- frontend/src/features/*/state/* (new)

Implementation plan:
- Model collaboration and document editing/review flows as explicit state machines.
- Encode legal transitions and side effects through machine actions.

Acceptance criteria:
- Workflow transitions are explicit and constrained.
- Regression risk from invalid UI state combinations is reduced.

------------------------------------------------

Task 78: Contract-first generated API clients (P4)

Status:
- Completed (2026-02-28).

Why:
- Transport contracts are partially hand-maintained, increasing drift risk.

Primary files:
- backend/app/main.py
- frontend/src/lib/api/*
- frontend/src/types/index.ts
- scripts/* (client generation tooling)

Implementation plan:
- Generate typed API clients/DTOs from OpenAPI contracts.
- Integrate generation into build/test flow.
- Keep mapper layer (Task 63) for transport-domain adaptation.

Acceptance criteria:
- Core API DTO types are generated from contract source.
- Contract drift between backend and frontend is reduced.

------------------------------------------------

Task 79: Cross-service contract tests (P4)

Status:
- Completed (2026-02-28).

Why:
- Token/payload compatibility across backend, frontend, and collab-server relies on implicit assumptions.

Primary files:
- backend/tests/contracts/* (new)
- frontend/src/test/contracts/* (new)
- collab-server/src/__tests__/*

Implementation plan:
- Add contract tests for auth token claims, collaboration payloads, and key API response shapes.
- Run contract suite in CI across all three applications.

Acceptance criteria:
- Breaking contract changes are caught by automated tests before merge.
- Shared payload assumptions are explicitly versioned/tested.

------------------------------------------------

Task 80: Scenario object mothers and cross-domain fixture packs (P4)

Status:
- Not started.

Why:
- Task 65 adds base builders; advanced cross-domain scenarios still need reusable high-level fixtures.

Primary files:
- backend/tests/factories/*
- backend/tests/scenarios/* (new)
- frontend/src/test/scenarios/* (new)
- collab-server/src/__tests__/scenarios/* (new)

Implementation plan:
- Add scenario-level object mothers for common end-to-end domain setups.
- Standardize fixture packs for tenant/user/document/review/collaboration combinations.

Acceptance criteria:
- Multi-domain tests can be written with minimal setup noise.
- Scenario fixtures encode business intent clearly.

------------------------------------------------

Task 81: Architecture fitness tests in CI (P4)

Status:
- Completed (2026-02-27).

Why:
- Planned OOP boundaries can regress without automated structural checks.

Primary files:
- backend/ (import/layer checks)
- frontend/ (feature boundary checks)
- collab-server/ (module boundary checks)
- .github/workflows/*

Implementation plan:
- Add architecture tests for forbidden imports and layer rules.
- Run them in CI as required checks.

Acceptance criteria:
- Boundary violations fail CI deterministically.
- Layer/context import rules are versioned and documented.

------------------------------------------------

Task 82: Class-based controller handlers by context (P4)

Status:
- Completed (2026-02-27).

Why:
- Route handlers still mix transport adaptation with use-case orchestration.

Primary files:
- backend/app/api/management/*
- backend/app/api/portal/*
- backend/app/controllers/* (new)

Implementation plan:
- Introduce controller classes per context (documents, reviews, users, portal).
- Keep route modules thin and delegate to controller methods.

Acceptance criteria:
- Controllers own orchestration; routes primarily map HTTP I/O.
- Repeated endpoint logic is reduced in targeted contexts.

------------------------------------------------

Task 83: Domain factories for complex creation flows (P4)

Status:
- Completed (2026-02-27).

Why:
- Multi-object creation flows are repeated across services and routes.

Primary files:
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- backend/app/services/invitations* (where applicable)
- backend/app/domain/factories/* (new)

Implementation plan:
- Add factories for complex aggregate initialization.
- Use factories from command handlers/services instead of ad-hoc constructor logic.

Acceptance criteria:
- Complex object graph creation is centralized and reusable.
- Initialization invariants are enforced in one place.

------------------------------------------------

Task 84: Capability objects for authorization (P4)

Status:
- Completed (2026-02-27).

Why:
- Authorization logic is currently role/permission-string heavy and spread across modules.

Primary files:
- backend/app/services/permissions.py
- backend/app/dependencies/permissions.py
- backend/app/security.py
- backend/app/domain/capabilities/* (new)

Implementation plan:
- Represent authorization as capability objects (`CanPublish`, `CanAssignCompanies`, etc.).
- Resolve capabilities from role/policy context, then consume in handlers/services.

Acceptance criteria:
- Core authorization checks use capability objects instead of scattered raw role checks.
- Capability mapping and decisions are unit-tested.

------------------------------------------------

Task 85: Plugin registry for extension points (P4)

Status:
- Completed (2026-02-28).

Why:
- Adding converters/exporters/channels currently requires direct edits in core modules.

Primary files:
- backend/app/utils/document_converter.py
- backend/app/api/management/analytics.py
- backend/app/services/email_service.py
- backend/app/plugins/* (new)

Implementation plan:
- Create plugin registry and plugin interfaces for converters/exporters/notification channels.
- Replace hardcoded switch paths with registry lookups.

Acceptance criteria:
- New plugin implementations can be registered without core-logic edits.
- Registry lifecycle (load/validate/select) is tested.

------------------------------------------------

Task 86: Anti-corruption layers for external contracts (P4)

Status:
- Completed (2026-02-28).

Why:
- External payload/token schemas can leak directly into domain/application layers.

Primary files:
- collab-server/src/auth.ts
- collab-server/src/persistence.ts
- backend/app/services/collaboration_service.py
- backend/app/adapters/* (new)

Implementation plan:
- Add anti-corruption mappers/adapters at external boundaries (collab, SMTP, storage, generated clients).
- Keep domain models independent from external contract drift.

Acceptance criteria:
- External schema changes are absorbed in adapter layers.
- Domain/application contracts remain stable.

------------------------------------------------

Task 87: Immutable command/query DTOs (P4)

Status:
- Completed (2026-02-27).

Why:
- Mutable payload objects increase accidental state mutation risk in orchestration flows.

Primary files:
- backend/app/application/commands/*
- backend/app/application/queries/*
- backend/app/schemas/*
- frontend/src/features/* (if shared command models exist)

Implementation plan:
- Use immutable DTO types for app-layer command/query payloads.
- Enforce no in-place mutation in handler flows.

Acceptance criteria:
- Application command/query DTOs are immutable by construction.
- Handler logic is updated to treat DTOs as read-only inputs.

------------------------------------------------

Task 88: Workflow-stage state objects (P4)

Status:
- Completed (2026-02-27).

Why:
- Enum + conditional chains are still common for lifecycle handling.

Primary files:
- backend/app/services/version_service.py
- backend/app/api/management/reviews.py
- frontend/src/pages/document-detail/hooks/*
- backend/app/domain/states/* (new)

Implementation plan:
- Model lifecycle behavior with stage/state objects and transition methods.
- Use stage objects in workflows that currently rely on conditional trees.

Acceptance criteria:
- Workflow logic uses explicit stage behavior objects.
- Invalid transition paths are structurally harder to express.

------------------------------------------------

Task 89: Use-case metrics and tracing objects (P4)

Status:
- Not started.

Why:
- Observability is mostly log-based and not consistently tied to use-case boundaries.

Primary files:
- backend/app/application/commands/*
- backend/app/application/queries/*
- backend/app/observability/* (new)
- collab-server/src/*

Implementation plan:
- Add use-case telemetry objects/interceptors for timing, outcome, and key dimensions.
- Standardize trace/metric naming across services.

Acceptance criteria:
- Core command/query handlers emit consistent metrics/traces.
- Failures and latency hotspots are attributable by use-case identifier.

------------------------------------------------

Task 90: Selective event-sourcing pilot (P5)

Status:
- Not started.

Why:
- Certain high-complexity workflows may benefit from immutable event history and replay.

Primary files:
- backend/app/domain/events/*
- backend/app/models/__init__.py
- backend/app/event_store/* (new)
- backend/tests/*

Implementation plan:
- Run feasibility analysis and pilot one bounded domain (not whole system).
- Define go/no-go criteria (complexity, performance, operational cost).
- If approved, implement minimal event-store + projection path for pilot.

Acceptance criteria:
- Decision record exists with measured tradeoffs and recommendation.
- Pilot implementation (if approved) has tests and rollback strategy.

------------------------------------------------

Task 91: Architecture Decision Record workflow (P5)

Status:
- Completed (2026-02-27).

Why:
- Large OOP/design refactors need durable decision history and rationale.

Primary files:
- docs/adr/* (new)
- CONTRIBUTING.md
- .github/pull_request_template.md (or equivalent)

Implementation plan:
- Create ADR template and naming/versioning conventions.
- Require ADRs for major architecture changes (waves E-O scope).
- Add PR checklist item linking the relevant ADR.

Acceptance criteria:
- ADR directory and template exist.
- Contribution/PR workflow documents when ADRs are required.
- New architecture changes include ADR linkage.

------------------------------------------------

Task 92: Deprecation lifecycle policy (P5)

Status:
- Completed (2026-02-27).

Why:
- Refactors across APIs/modules need controlled retirement path to avoid abrupt breaks.

Primary files:
- docs/deprecation-policy.md (new)
- backend/app/api/*
- frontend/src/lib/api/*
- collab-server/src/*

Implementation plan:
- Define staged lifecycle: proposed -> deprecated -> warned -> removed.
- Define owner, timeline, compatibility window, and communication rules.
- Add runtime/CI deprecation markers where appropriate.

Acceptance criteria:
- Deprecation policy doc is published and referenced in CONTRIBUTING.
- Deprecated paths have explicit owner and removal target.
- CI/docs include visibility for active deprecations.

------------------------------------------------

Task 93: Internal package/layer split (P5)

Status:
- Completed (2026-02-27).

Why:
- Physical structure still allows cross-layer coupling that contradicts target architecture.

Primary files:
- backend/app/*
- frontend/src/*
- collab-server/src/*

Implementation plan:
- Split code into explicit packages/layers (`domain`, `application`, `infrastructure`, `web`).
- Move modules gradually behind stable interfaces.
- Pair with architecture-fitness checks (Task 81) to enforce boundaries.

Acceptance criteria:
- Targeted modules live under explicit layer packages.
- Cross-layer imports follow allowed dependency direction only.

------------------------------------------------

Task 94: Refactor-budget CI gates (P5)

Status:
- Completed (2026-02-27).

Why:
- Large, risky changes can reintroduce complexity without guardrails.

Primary files:
- .github/workflows/*
- scripts/*
- backend/*
- frontend/*

Implementation plan:
- Define budgets for touched files (max size delta, complexity delta, new warning thresholds).
- Add CI checks and documented override workflow.

Acceptance criteria:
- PRs violating budgets fail with actionable feedback.
- Exception path exists with explicit owner approval.

------------------------------------------------

Task 95: Fault-injection resilience tests for adapters (P5)

Status:
- Not started.

Why:
- External systems (SMTP/storage/collab APIs) fail in ways not fully covered by happy-path tests.

Primary files:
- backend/tests/*
- collab-server/src/__tests__/*
- backend/app/adapters/*

Implementation plan:
- Add failure-mode tests (timeouts, partial failures, malformed payloads, retries exhausted).
- Validate fallback and compensation behavior for critical flows.

Acceptance criteria:
- Core adapter failure modes are reproducibly tested.
- Recovery behavior is explicit and verified.

------------------------------------------------

Task 96: Wave migration playbooks/checklists (P5)

Status:
- Completed (2026-02-27).

Why:
- Multi-wave architecture migration needs repeatable, low-risk execution steps.

Primary files:
- docs/migrations/* (new)
- CONTRIBUTING.md
- plan

Implementation plan:
- Publish per-wave playbooks (pre-checks, change steps, validation, rollback steps).
- Link playbooks from plan and contributor docs.

Acceptance criteria:
- Each active wave has an associated migration checklist.
- Teams can execute/refactor using documented procedure.

------------------------------------------------

Task 97: Internal contract versioning policy (P5)

Status:
- Completed (2026-02-27).

Why:
- Payload/token contract changes across backend/frontend/collab need explicit versioning strategy.

Primary files:
- docs/contracts/versioning.md (new)
- backend/app/*
- frontend/src/lib/api/*
- collab-server/src/*

Implementation plan:
- Define versioning rules, compatibility windows, and migration timelines.
- Add contract version metadata and policy checks where applicable.

Acceptance criteria:
- Contract changes follow documented version policy.
- Backward compatibility expectations are explicit per release.

------------------------------------------------

Task 98: Use-case performance SLOs (P5)

Status:
- Not started.

Why:
- Architectural improvements need measurable latency/reliability targets.

Primary files:
- docs/slo/*
- backend/app/observability/*
- .github/workflows/*

Implementation plan:
- Define SLOs for key use-cases (p95 latency, error budget).
- Instrument command/query paths and publish dashboards/alerts.

Acceptance criteria:
- Named SLOs exist for critical use-cases.
- Telemetry can show SLO compliance trends over time.

------------------------------------------------

Task 99: Feature-toggle framework for architecture rollout (P5)

Status:
- Completed (2026-02-27).

Why:
- Large refactors need controlled release and fast rollback without redeploy risk.

Primary files:
- backend/app/config.py
- backend/app/*
- frontend/src/config/*
- frontend/src/*

Implementation plan:
- Add feature-flag framework for backend/frontend architecture toggles.
- Use flags to roll out new pathways gradually and revert safely.

Acceptance criteria:
- Refactor features can be enabled/disabled by config without code edits.
- Rollout/rollback procedure is documented and tested.

------------------------------------------------

Task 100: No direct DB access in routes enforcement (P5)

Status:
- Completed (2026-02-27).

Why:
- Route-level ORM access bypasses service/repository abstractions and weakens OOP boundaries.

Primary files:
- backend/app/api/*
- backend/app/services/*
- backend/app/repositories/*
- scripts/* (lint/codemod support)

Implementation plan:
- Add lint/check rule to detect direct `db.query` usage in route modules.
- Provide codemod guidance/helpers for migration to services/repositories.

Acceptance criteria:
- New route code cannot introduce direct DB access.
- Existing direct accesses are tracked and reduced over time with automated checks.

------------------------------------------------

Task 101: Idempotency keys for write operations (P6)

Status:
- Completed (2026-02-27).

Why:
- Client retries can create duplicate writes and duplicated side effects.

Primary files:
- backend/app/api/management/*
- backend/app/api/portal/*
- backend/app/services/*
- backend/app/models/__init__.py

Implementation plan:
- Add idempotency-key contract for selected POST/PUT/PATCH endpoints.
- Persist request/result fingerprints and enforce replay-safe behavior.
- Cover side-effecting flows (publish, invite, upload, comment/review actions).

Acceptance criteria:
- Duplicate requests with same idempotency key are safely deduplicated.
- Side effects are not executed twice on retried requests.

------------------------------------------------

Task 102: Central Policy Decision Point service (P6)

Status:
- Completed (2026-02-27).

Why:
- Authorization logic is still distributed across routes, dependencies, and services.

Primary files:
- backend/app/services/permissions.py
- backend/app/dependencies/permissions.py
- backend/app/security.py
- backend/app/policy/* (new)

Implementation plan:
- Implement PDP service taking subject/action/resource/context.
- Route authorization checks through PDP adapters in services/dependencies.
- Return structured allow/deny + reason payload.

Acceptance criteria:
- Core authorization checks use centralized PDP decisions.
- Permission behavior remains consistent across route and service layers.

------------------------------------------------

Task 103: Tenant isolation attack harness (P6)

Status:
- Not started.

Why:
- Multi-tenant regressions can slip in when new endpoints are added.

Primary files:
- backend/tests/tenant_isolation/* (new)
- backend/tests/conftest.py
- backend/app/api/*

Implementation plan:
- Build reusable tenant attack harness for common endpoint patterns.
- Auto-generate/execute cross-tenant read/write attempts for protected routes.
- Add CI gate for harness suite.

Acceptance criteria:
- New protected endpoints are covered by tenant isolation tests.
- Cross-tenant access regressions are caught automatically.

------------------------------------------------

Task 104: Data ownership map by bounded context (P7)

Status:
- Completed (2026-02-27).

Why:
- Write ownership between contexts is implicit and can drift.

Primary files:
- docs/context-ownership.md (new)
- docs/context-map/* (new)
- backend/app/*

Implementation plan:
- Document single write-owner per aggregate/entity group.
- Mark allowed read-only cross-context dependencies.
- Align repository/application layers with documented ownership.

Acceptance criteria:
- Ownership map exists and is referenced in architecture docs.
- Cross-context writes outside owner context are explicitly flagged.

------------------------------------------------

Task 105: Projection/caching architecture for heavy reads (P7)

Status:
- Completed (2026-02-27).

Why:
- Analytics/search/portal reads remain query-heavy and expensive under growth.

Primary files:
- backend/app/api/management/analytics.py
- backend/app/api/management/search.py
- backend/app/api/portal/documents.py
- backend/app/projections/* (new)

Implementation plan:
- Define read projections for heavy dashboards/search summaries.
- Invalidate/update projections via domain events.
- Add cache consistency checks and fallback behavior.

Acceptance criteria:
- Heavy read paths use projections/caches with deterministic invalidation.
- Read latency and DB load improve in measured benchmarks.

------------------------------------------------

Task 106: Unified async job framework (P7)

Status:
- Completed (2026-02-27).

Why:
- Async/background processing is fragmented across workflows.

Primary files:
- backend/app/workers/*
- backend/app/services/conversion_jobs.py
- backend/app/services/*
- backend/app/jobs/* (new)

Implementation plan:
- Standardize job model (retry policy, DLQ, poison handling, observability).
- Migrate key background flows to unified framework incrementally.

Acceptance criteria:
- Async workflows use shared retry/error/dead-letter semantics.
- Failed jobs are visible and recoverable with clear operational tooling.

------------------------------------------------

Task 107: Optimistic concurrency controls (P6)

Status:
- Completed (2026-02-27).

Why:
- Concurrent updates can overwrite each other without conflict detection.

Primary files:
- backend/app/models/__init__.py
- backend/app/services/document_service.py
- backend/app/services/version_service.py
- frontend/src/lib/api/*

Implementation plan:
- Add version/etag fields for mutable resources.
- Require precondition checks on updates (`If-Match` or version token).
- Return conflict responses when stale writes are detected.

Acceptance criteria:
- Lost-update scenarios are detected and rejected.
- UI/API flows surface actionable conflict handling.

------------------------------------------------

Task 108: Strangler wrappers for legacy modules (P7)

Status:
- Completed (2026-02-27).

Why:
- High-risk legacy modules are difficult to replace safely in one step.

Primary files:
- backend/app/utils/document_converter.py
- backend/app/services/analytics_service.py
- backend/app/legacy_wrappers/* (new)

Implementation plan:
- Introduce wrapper interfaces around legacy modules.
- Route new functionality through wrappers while gradually replacing internals.
- Track migration completion and legacy call volume.

Acceptance criteria:
- Legacy-heavy modules are accessed through wrapper boundaries.
- Incremental replacement can proceed with low regression risk.

------------------------------------------------

Task 109: Frontend feature-level dependency rules (P6)

Status:
- Completed (2026-02-27).

Why:
- Cross-feature imports can bypass boundaries and create hidden coupling.

Primary files:
- frontend/src/features/*
- frontend/eslint configuration
- frontend/tsconfig.json

Implementation plan:
- Define allowed dependency graph between feature modules.
- Enforce with lint/import rules and CI checks.

Acceptance criteria:
- Unauthorized cross-feature imports fail lint/CI.
- Feature boundaries are explicit and stable.

------------------------------------------------

Task 110: Backend-for-Frontend (BFF) layer (P7)

Status:
- Completed (2026-02-28).

Why:
- Complex UI pages orchestrate many backend calls and perform client-side joining.

Primary files:
- backend/app/api/bff/* (new)
- frontend/src/pages/*
- frontend/src/lib/api/*

Implementation plan:
- Add BFF endpoints for high-complexity pages first.
- Shift orchestration and response shaping to server-side BFF handlers.

Acceptance criteria:
- Targeted pages use fewer API round-trips and less client orchestration logic.
- BFF contracts remain aligned with core domain services.

------------------------------------------------

Task 111: Explicit use-case service interfaces (P6)

Status:
- Completed (2026-02-27).

Why:
- Service contracts are still partly CRUD-shaped rather than business use-case shaped.

Primary files:
- backend/app/services/*
- backend/app/application/interfaces/* (new)

Implementation plan:
- Define interfaces named by use-case intent (`PublishApprovedVersion`, `AssignCompanySet`, etc.).
- Implement/adapt existing services behind use-case contracts.

Acceptance criteria:
- New app-layer calls target use-case interfaces instead of generic CRUD methods.
- Use-case contracts improve readability and testability.

------------------------------------------------

Task 112: Reusable invariant specification objects (P6)

Status:
- Completed (2026-02-27).

Why:
- Invariant logic is repeated across runtime checks and test assertions.

Primary files:
- backend/app/domain/specifications/*
- backend/app/services/*
- backend/tests/*

Implementation plan:
- Encode invariants as reusable specification objects.
- Use the same specs in domain checks and test helpers.

Acceptance criteria:
- Core invariants are defined once and reused.
- Divergence between implementation checks and tests is reduced.

------------------------------------------------

Task 113: Command/query bus middleware pipeline (P6)

Status:
- Completed (2026-02-27).

Why:
- Cross-cutting concerns (validation/auth/tracing) are still manually repeated.

Primary files:
- backend/app/application/commands/*
- backend/app/application/queries/*
- backend/app/application/bus/* (new)

Implementation plan:
- Introduce command bus/query bus with middleware chain.
- Move validation/auth/tracing concerns to middleware layers.

Acceptance criteria:
- Command/query handlers are thinner and focused on domain logic.
- Cross-cutting concerns execute consistently via middleware.

------------------------------------------------

Task 114: Policy explanation objects (P6)

Status:
- Completed (2026-02-27).

Why:
- Authorization failures often return opaque deny outcomes.

Primary files:
- backend/app/policy/*
- backend/app/services/permissions.py
- backend/app/dependencies/permissions.py

Implementation plan:
- Add structured explanation objects for allow/deny decisions.
- Use explanations in API errors/audit logs where appropriate.

Acceptance criteria:
- Permission denials can expose consistent human-readable reason codes.
- Audit/debug workflows can trace decision rationale.

------------------------------------------------

Task 115: Context map artifacts (P7)

Status:
- Completed (2026-02-27).

Why:
- Upstream/downstream domain relationships are not explicitly maintained.

Primary files:
- docs/context-map/* (new)
- docs/adr/*
- plan

Implementation plan:
- Produce context-map diagrams/documents for major bounded contexts.
- Link context-map references in ADRs and migration playbooks.

Acceptance criteria:
- Context-map artifacts exist and are maintained with architecture changes.
- Cross-context dependencies are easier to review and govern.

------------------------------------------------

Task 116: Executable workflow models + model-based tests (P6)

Status:
- Completed (2026-02-27).

Why:
- High-risk workflows remain partially defined by imperative condition chains.

Primary files:
- backend/app/domain/workflows/*
- backend/tests/workflows/* (new)
- frontend/src/features/*/state/*

Implementation plan:
- Model critical workflows as executable state machines.
- Add model-based tests to verify transition completeness and invariants.

Acceptance criteria:
- Key workflows are represented by formal models with exhaustive transition tests.
- Regression risk in lifecycle logic is reduced.

------------------------------------------------

Task 117: Consumer-driven contract tests (P6)

Status:
- Completed (2026-02-28).

Why:
- Backend/provider changes can break consumer assumptions even with type checks.

Primary files:
- backend/tests/contracts/*
- frontend/src/test/contracts/*
- .github/workflows/*

Implementation plan:
- Define consumer contracts for critical frontend API usages.
- Run provider verification in backend CI.

Acceptance criteria:
- Contract mismatches fail CI before integration regressions reach production.
- Consumer/provider compatibility is explicitly versioned/tested.

------------------------------------------------

Task 118: Migration safety framework (P7)

Status:
- Completed (2026-02-27).

Why:
- Schema/architecture migrations need stronger safety checks and rollback confidence.

Primary files:
- backend/alembic/*
- scripts/migration_safety/* (new)
- docs/migrations/*

Implementation plan:
- Add dry-run validation, preflight guards, rollback probes, post-migration assertions.
- Integrate into release/migration pipeline.

Acceptance criteria:
- Migrations include safety checks and rollback validation steps.
- High-risk migrations have reproducible safety evidence.

------------------------------------------------

Task 119: SLO burn-rate alerts by use-case (P7)

Status:
- Not started.

Why:
- Raw SLO metrics are less actionable without burn-rate based alerting.

Primary files:
- backend/app/observability/*
- docs/slo/*
- infra/alerting configs (where applicable)

Implementation plan:
- Define burn-rate alert thresholds tied to use-case SLO budgets.
- Wire alerts and escalation runbooks.

Acceptance criteria:
- Burn-rate alerts exist for critical use-cases.
- Alert runbooks are documented and tested.

------------------------------------------------

Task 120: Chaos testing for adapter boundaries (P7)

Status:
- Not started.

Why:
- Staging-level resilience under real fault patterns is not systematically validated.

Primary files:
- scripts/chaos/* (new)
- backend/tests/*
- collab-server/tests/*
- CI/staging pipeline definitions

Implementation plan:
- Add controlled chaos scenarios for SMTP/storage/collab boundaries in staging.
- Measure recovery and failure containment behavior.

Acceptance criteria:
- Staging chaos suite runs on schedule/release candidates.
- Recovery behavior is measurable and within defined thresholds.

------------------------------------------------

Task 121: Architecture debt register (P6)

Status:
- Completed (2026-02-27).

Why:
- Architecture debt lacks standardized ownership and risk tracking.

Primary files:
- docs/architecture-debt.md (new)
- scripts/architecture_debt_checks/* (new)
- .github/workflows/*

Implementation plan:
- Track debt items with owner, risk score, due date, and mitigation plan.
- Add CI reminder/reporting on overdue high-risk items.

Acceptance criteria:
- Debt register is maintained and linked from planning docs.
- High-risk overdue items are visible in CI reporting.

------------------------------------------------

Task 122: Refactor scaffolding generators/templates (P7)

Status:
- Not started.

Why:
- Repeated architectural patterns are currently implemented manually and inconsistently.

Primary files:
- scripts/scaffold/* (new)
- backend/app/*
- frontend/src/*
- collab-server/src/*

Implementation plan:
- Create generators/templates for new service/repository/policy/controller modules.
- Include tests/docs stubs and boundary-compliant defaults.

Acceptance criteria:
- New architecture components can be scaffolded consistently.
- Generated outputs follow agreed structure and quality baseline.

================================================
2.1) Impact x Effort Ranking (Tasks 101-122)
================================================

Scoring model:
- Impact: 1 (low) to 5 (very high).
- Effort: 1 (low) to 5 (very high).
- Priority score: Impact / Effort (higher = earlier execution value).

ROI ranking (efficiency signal only; not dependency order):
1. Task 102 - Impact 5, Effort 2, Score 2.50.
2. Task 109 - Impact 4, Effort 2, Score 2.00.
3. Task 111 - Impact 4, Effort 2, Score 2.00.
4. Task 112 - Impact 4, Effort 2, Score 2.00.
5. Task 117 - Impact 4, Effort 2, Score 2.00.
6. Task 101 - Impact 5, Effort 3, Score 1.67.
7. Task 103 - Impact 5, Effort 3, Score 1.67.
8. Task 107 - Impact 5, Effort 3, Score 1.67.
9. Task 104 - Impact 3, Effort 2, Score 1.50.
10. Task 114 - Impact 3, Effort 2, Score 1.50.
11. Task 115 - Impact 3, Effort 2, Score 1.50.
12. Task 121 - Impact 3, Effort 2, Score 1.50.
13. Task 113 - Impact 4, Effort 3, Score 1.33.
14. Task 105 - Impact 4, Effort 4, Score 1.00.
15. Task 106 - Impact 4, Effort 4, Score 1.00.
16. Task 108 - Impact 4, Effort 4, Score 1.00.
17. Task 116 - Impact 4, Effort 4, Score 1.00.
18. Task 118 - Impact 4, Effort 4, Score 1.00.
19. Task 120 - Impact 4, Effort 4, Score 1.00.
20. Task 119 - Impact 3, Effort 3, Score 1.00.
21. Task 122 - Impact 3, Effort 3, Score 1.00.
22. Task 110 - Impact 3, Effort 4, Score 0.75.

Dependency-first execution order is defined by Waves D-O in section 1.

================================================
3) Validation Matrix
================================================

Backend tests:
- backend/tests/test_documents.py
- backend/tests/test_permissions.py
- backend/tests/test_roles.py
- Additional targeted tests for tenant-scoped company assignment and delete transaction rollback.
- Additional policy/repository/unit-of-work tests for Tasks 50-52.
- Additional domain-event/value-object/workflow tests for Tasks 53-56.
- Additional notification/auth/conversion/collaboration architecture tests for Tasks 57-61.
- Additional mapper/error/factory tests for Tasks 63-65.
- Additional bounded-context/aggregate/ports/specification/command-query tests for Tasks 66-71.
- Additional outbox/saga/result/container architecture tests for Tasks 72-75.
- Additional generated-client/contract/scenario tests for Tasks 78-80.
- Additional architecture-fitness/controller/factory/capability tests for Tasks 81-84.
- Additional plugin/ACL/immutable-dto/state-object tests for Tasks 85-88.
- Additional use-case telemetry tests and event-sourcing pilot validation for Tasks 89-90.
- Additional ADR/deprecation policy compliance checks for Tasks 91-92.
- Additional package-boundary and route-DB-enforcement checks for Tasks 93 and 100.
- Additional fault-injection and resilience regression suites for Task 95.
- Additional contract-versioning and SLO compliance checks for Tasks 97-98.
- Additional idempotency and optimistic-concurrency tests for Tasks 101 and 107.
- Additional PDP/policy-explanation/invariant tests for Tasks 102, 112, and 114.
- Additional tenant-isolation attack-harness checks for Task 103.
- Additional use-case interface and bus-pipeline tests for Tasks 111 and 113.
- Additional executable workflow and model-based tests for Task 116.
- Additional projection/cache, async-job, and strangler parity tests for Tasks 105, 106, and 108.
- Additional migration safety, burn-rate alert, and chaos boundary tests for Tasks 118-120.
- Additional scaffolding generator output checks for Task 122.

Frontend tests:
- Add/update tests for Documents page filters and role-gated actions.
- Add tests for extracted hooks/components from Task 47.
- Add controller/view-model tests for Task 62.
- Add API DTO mapping tests for Task 63.
- Add feature-module and workflow-machine tests for Tasks 76-77.
- Add generated-client integration tests for Task 78.
- Add architecture boundary checks and immutable DTO usage tests for Tasks 81 and 87.
- Add feature-toggle behavior tests for Task 99.
- Add feature-level dependency rule tests for Task 109.
- Add BFF integration and DTO boundary tests for Task 110.
- Add policy explanation rendering/handling tests for Task 114.
- Add consumer-side contract tests for Task 117.

Collab-server tests:
- collab-server/src/__tests__/auth.test.ts
- collab-server/src/__tests__/persistence.test.ts
- Additional tests for new connection-registry/bootstrap classes.
- Additional auth contract tests for Task 58.
- Additional cross-service contract tests for Task 79.
- Additional anti-corruption and telemetry integration tests for Tasks 86 and 89.
- Additional adapter fault-injection and contract-versioning tests for Tasks 95 and 97.
- Additional provider contract checks for Task 117.
- Additional BFF/collab payload compatibility tests for Task 110.
- Additional chaos/partition recovery checks for Task 120.

Manual smoke checklist:
1. Assigned-companies operations with same-tenant and cross-tenant users.
2. Documents list filtering by search, status, and visibility together.
3. Role-based delete availability and delete flow behavior.
4. Document delete audit consistency under forced failure conditions.
5. Collaboration server startup, health, connect/disconnect, and persistence behavior.
6. Generated client compatibility with backend OpenAPI contract.
7. Cross-service token/payload compatibility checks (backend/frontend/collab).
8. Multi-step workflow recovery behavior under forced failures (outbox/saga flows).
9. CI architecture fitness checks fail on intentional boundary violations.
10. Plugin registration/dynamic resolution behavior for converters/exporters/channels.
11. Capability-based authorization decisions match policy matrix behavior.
12. Telemetry output includes per-use-case traces/metrics for core command/query paths.
13. Event-sourcing pilot (if enabled) supports replay and projection consistency.
14. ADR requirement is enforced for major architecture PRs.
15. Deprecation lifecycle status is visible for deprecated APIs/modules.
16. Package/layer boundaries are physically enforced and verified in CI.
17. Fault-injection scenarios validate resilience for SMTP/storage/collab adapters.
18. Contract versioning behavior remains compatible across backend/frontend/collab.
19. SLO telemetry shows p95/error-budget status for key use-cases.
20. Feature flags can safely enable/disable refactor pathways.
21. Route-layer direct `db.query` calls are blocked by automated checks.
22. Idempotent retries for write endpoints do not duplicate side effects.
23. PDP authorization decisions and explanation payloads match policy expectations.
24. Tenant-isolation attack harness shows no cross-tenant data access path.
25. Concurrent update conflicts are detected and handled via version/etag checks.
26. Frontend dependency rules block forbidden cross-feature imports.
27. Command/query middleware ordering is traceable and deterministic.
28. Workflow model-based tests pass for critical lifecycle flows.
29. Projection/cache invalidation stays consistent after write operations.
30. Async job retries, DLQ handling, and poison-message behavior are observable.
31. Strangler wrappers preserve behavior parity with legacy module paths.
32. BFF aggregation endpoints enforce permissions and contract shape correctly.
33. Data ownership and context-map artifacts are up-to-date and reviewed.
34. Migration dry-run/preflight/rollback probes pass before release.
35. Burn-rate alerts fire under synthetic error-budget exhaustion.
36. Adapter-boundary chaos scenarios recover within defined thresholds.
37. Architecture debt register surfaces overdue high-risk items.
38. Scaffold-generated modules compile and include baseline tests/docs stubs.

================================================
4) Definition of Done
================================================

For each task:
1. Code changes implemented.
2. Tests added or updated.
3. Typecheck/lint clean for touched files (or no new warnings introduced).
4. Progress note added below.

================================================
5) Progress
================================================

2026-02-27:
- Completed Task 60: introduced strategy-driven conversion package under `backend/app/conversion/`:
- `DocumentConversionPipeline` with explicit converter strategies (`PdfConverterStrategy`, `WordConverterStrategy`, `TextConverterStrategy`, `HtmlPassthroughStrategy`) and typed conversion requests.
- `PreviewPdfConversionPipeline` with conversion strategies (`ImagePreviewPdfStrategy`, `OfficePreviewPdfStrategy`, `HtmlPreviewPdfStrategy`, `TextPreviewPdfStrategy`, `GenericDocumentPreviewPdfStrategy`) for non-PDF preview artifact generation.
- Refactored document-converter wrapper to delegate through the new conversion pipeline boundary while preserving existing strangler-wrapper call tracking (`backend/app/legacy_wrappers/document_converter.py`).
- Refactored attachment preview conversion orchestration to use strategy pipeline dispatch instead of inline MIME branching (`backend/app/services/attachment_service/artifacts.py`), preserving existing Office/Word fallback and error semantics.
- Kept utility API compatibility by routing `convert_document_to_html(...)` through the new conversion pipeline (`backend/app/utils/document_converter.py`).
- Added conversion pipeline regression coverage in `backend/tests/test_conversion_pipeline.py` and updated wrapper regression mocks in `backend/tests/test_legacy_wrappers.py`.
- Task 60 validation run:
- `docker compose run --rm backend ruff check app/conversion app/legacy_wrappers/document_converter.py app/services/attachment_service/artifacts.py app/utils/document_converter.py tests/test_conversion_pipeline.py tests/test_legacy_wrappers.py tests/test_attachment_office_conversion.py tests/test_document_converter_reader.py tests/test_document_converter_toc.py tests/test_attachments.py` (pass)
- `docker compose run --rm backend pytest tests/test_conversion_pipeline.py tests/test_legacy_wrappers.py tests/test_attachment_office_conversion.py tests/test_document_converter_reader.py tests/test_document_converter_toc.py -q` (pass)
- `docker compose run --rm backend pytest tests/test_attachments.py -q` (pass)
- Completed Task 57: introduced notification pipeline object model under `backend/app/notifications/` with explicit message objects (`NotificationMessage`), template objects (`DocumentPublishedTemplate`, `NewCommentTemplate`, `CommentReplyTemplate`, `PasswordResetTemplate`, `WelcomeTemplate`), delivery channels (`EmailNotificationChannel`), and dispatcher facade (`NotificationDispatcher`).
- Refactored `EmailService` to focus on transport concerns plus message dispatch wrappers, with subject/body rendering moved into template objects (`backend/app/services/email_service.py`).
- Refactored domain-event side effects to use the notification dispatcher facade instead of direct template/transport calls (`backend/app/services/domain_event_handlers.py`), preserving recipient selection behavior.
- Added notification pipeline regression coverage in `backend/tests/test_notifications_pipeline.py` and kept existing email/domain-event/outbox behavior tests passing.
- Task 57 validation run:
- `docker compose run --rm backend ruff check app/notifications app/services/email_service.py app/services/domain_event_handlers.py tests/test_notifications_pipeline.py tests/test_email.py tests/test_domain_events.py tests/test_outbox.py` (pass)
- `docker compose run --rm backend pytest tests/test_notifications_pipeline.py tests/test_email.py tests/test_domain_events.py tests/test_outbox.py -q` (pass)
- `docker compose run --rm backend pytest tests/test_comments.py tests/test_versions.py -q` (pass)
- Completed Task 58: introduced dedicated auth context services under `backend/app/auth_context/` (`TokenService`, `RefreshTokenService`, `CollaborationAuthService`) plus centralized token payload contracts and shared password hashing helpers.
- Refactored backend auth/token call sites to delegate lifecycle responsibilities to auth context services (`backend/app/services/auth_service.py`, `backend/app/security.py`, `backend/app/services/collaboration_service.py`, `backend/app/api/management/auth.py`, `backend/app/application/contexts/collaboration/api.py`) while preserving existing route contracts and compatibility wrappers.
- Refactored collaboration server auth into explicit service + contract validation modules (`collab-server/src/authContext/collaborationAuthService.ts`, `collab-server/src/authContext/contracts.ts`) and kept `collab-server/src/auth.ts` as thin wrapper utilities for existing imports.
- Added auth contract regression coverage in `backend/tests/test_auth_context_services.py`, extended collaboration token contract assertions in `backend/tests/test_collaboration.py`, and added collab-server token-type validation coverage in `collab-server/src/__tests__/auth.test.ts`.
- Task 58 validation run:
- `docker compose run --rm backend ruff check app/auth_context app/security.py app/services/auth_service.py app/services/collaboration_service.py app/api/management/auth.py app/application/contexts/collaboration/api.py tests/test_auth_context_services.py tests/test_auth.py tests/test_security.py tests/test_collaboration.py` (pass)
- `docker compose run --rm backend pytest tests/test_auth_context_services.py tests/test_auth.py tests/test_security.py tests/test_collaboration.py -q` (pass)
- `docker compose run --rm backend pytest tests/test_dependencies_permissions.py -q` (pass)
- `npm.cmd --prefix collab-server run typecheck` (pass)
- `npm.cmd --prefix collab-server run test -- --runInBand` (pass)
- Completed Task 61: extracted collaboration backend responsibilities into focused manager service objects under `backend/app/collaboration/` (`CollabStateManager`, `SessionManager`, `SnapshotManager`) with shared access/permission checks.
- Refactored collaboration API handlers to thin input/output adapters delegating workflow logic to managers (`backend/app/api/management/collaboration/state.py`, `sessions.py`, `activity.py`, `snapshots.py`).
- Added manager-level unit coverage in `backend/tests/test_collaboration_managers.py` and preserved endpoint regression behavior in collaboration API suites.
- Task 61 validation run:
- `docker compose run --rm backend ruff check app/collaboration app/api/management/collaboration app/services/collaboration_service.py app/services/snapshot_service.py tests/test_collaboration.py tests/test_collaboration_managers.py` (pass)
- `docker compose run --rm backend pytest tests/test_collaboration_managers.py tests/test_collaboration.py -q` (pass)
- Completed Task 48: refactored `collab-server` bootstrap into explicit classes: `CollabServerApp`, `ConnectionRegistry`, and `HealthServer` (`collab-server/src/server/*`), with `src/index.ts` reduced to thin startup/shutdown wiring.
- Kept auth/persistence hooks behavior and env-driven runtime configuration intact while moving connection/auth tracking and health endpoint lifecycle into dedicated service objects.
- Added collab-server regression coverage for new class boundaries:
- `collab-server/src/__tests__/connectionRegistry.test.ts`
- `collab-server/src/__tests__/healthServer.test.ts`
- `collab-server/src/__tests__/collabServerApp.test.ts`
- Task 48 validation run:
- `npm.cmd --prefix collab-server run typecheck` (pass)
- `npm.cmd --prefix collab-server run test -- --runInBand` (pass)
- Completed Task 82: added class-based HTTP controllers for management users and portal documents contexts (`backend/app/web/controllers/management/users_controller.py`, `backend/app/web/controllers/portal/documents_controller.py`).
- Refactored route modules to thin transport adapters that delegate orchestration to controller methods while preserving endpoint contracts (`backend/app/api/management/users.py`, `backend/app/api/portal/documents.py`).
- Added controller regression coverage in `backend/tests/test_web_controllers.py`.
- Task 82 validation run:
- `docker compose run --rm backend ruff check app/app_factory.py app/web/router_registry.py app/web/__init__.py app/api/management/users.py app/api/portal/documents.py app/web/controllers tests/test_app_factory_router_registry.py tests/test_web_controllers.py` (pass)
- `docker compose run --rm backend pytest tests/test_app_factory_router_registry.py tests/test_web_controllers.py tests/test_roles.py tests/test_user_company_invariants.py tests/test_portal_api.py -q` (pass)
- Completed Task 59: refactored FastAPI bootstrap into explicit object model with `FastAPIAppFactory` and declarative `FastAPIRouterRegistry` registration entries (`backend/app/app_factory.py`, `backend/app/web/router_registry.py`, `backend/app/web/__init__.py`).
- Preserved backward-compatible `create_app()`/`register_routers()` entrypoints while moving composition responsibilities into class-based factory/registry methods.
- Added Task 59 regression coverage in `backend/tests/test_app_factory_router_registry.py` for default registry entries and custom registry injection behavior.
- Task 59 validation run:
- `docker compose run --rm backend ruff check app/app_factory.py app/web/router_registry.py app/web/__init__.py tests/test_app_factory_router_registry.py` (pass)
- `docker compose run --rm backend pytest tests/test_app_factory_router_registry.py tests/test_container.py tests/test_feature_flags.py -q` (pass)
- Completed Task 118: introduced migration safety framework runner at `scripts/migration_safety/run_migration_safety.py` with preflight guards, dry-run validation (ephemeral upgrade + schema SQL dump artifact), rollback probe (`upgrade head -> downgrade -1 -> re-upgrade head`), and post-migration schema assertions.
- Integrated migration safety enforcement into CI (`.github/workflows/ci.yml`) with dedicated `migration-safety` job, evidence artifact upload, and `ci-success` gate checks.
- Published migration safety docs/playbook references (`scripts/migration_safety/README.md`, `docs/migrations/migration-safety-framework.md`) and linked migration evidence expectations in existing migration docs.
- Task 118 validation run:
- `docker compose run --rm -v "${PWD}:/workspace" backend sh -lc "python /workspace/scripts/migration_safety/run_migration_safety.py --backend-dir /workspace/backend --report-file /workspace/docs/migrations/evidence/latest-migration-safety.json --dry-run-sql-file /workspace/docs/migrations/evidence/latest-dry-run.sql"` (pass)
- `docker compose run --rm -v "${PWD}:/workspace" backend sh -lc "ruff check /workspace/scripts/migration_safety/run_migration_safety.py"` (pass)
- Completed Task 41: assign/remove/list company assignment endpoints now use tenant-scoped document lookup via tenant context.
- Completed Task 42: visibility filter wired end-to-end (Documents page query key/request + backend documents list filter application).
- Completed Task 43: Documents page delete action aligned to manager+ permissions to match backend authorization.
- Completed Task 44: document delete and delete-audit logging are now atomic within a single transaction with rollback on failure.
- Added/updated regression coverage in `backend/tests/test_documents.py` for visibility filtering, cross-tenant company-assignment denial, and delete rollback consistency.
- Updated frontend e2e permission expectation in `frontend/e2e/permissions.spec.ts` (editor cannot see delete action in documents list).
- Validation run:
- `docker compose run --rm backend pytest tests/test_documents.py` (pass)
- `docker compose run --rm backend ruff check app tests/test_documents.py` (pass)
- `npm.cmd --prefix frontend run build` (pass)
- Completed Task 91: established ADR workflow (`docs/adr/README.md`, template, and ADR-0001 baseline decision).
- Completed Task 92: published deprecation lifecycle policy and active register (`docs/deprecation-policy.md`, `docs/deprecations.md`) with CI visibility checks.
- Completed Task 96: published per-wave migration playbooks/checklists for Waves E-O under `docs/migrations/`.
- Completed Task 97: published internal contract versioning policy at `docs/contracts/versioning.md`.
- Completed Task 104: published bounded-context data ownership map at `docs/context-ownership.md` and linked from architecture docs.
- Completed Task 115: published context-map artifacts in `docs/context-map/` and linked from ADR/migration guidance.
- Completed Task 121: added architecture debt register (`docs/architecture-debt.md`) and CI reporting scripts/workflow (`scripts/architecture_debt_checks/*`, `.github/workflows/architecture-governance.yml`).
- Updated contributor and PR workflows to require governance links/metadata for architecture-impacting changes (`CONTRIBUTING.md`, `.github/PULL_REQUEST_TEMPLATE.md`).
- Completed Wave F:
- Task 66: added bounded-context APIs under `backend/app/application/contexts/*` and explicit backend layer packages.
- Task 68: added ports/adapters for email/storage/collaboration state and injected collaboration state persistence via adapter port.
- Task 81: added architecture fitness checks and CI workflow (`scripts/architecture_checks/*`, `.github/workflows/architecture-fitness.yml`).
- Task 93: added explicit layer/package scaffolding in backend/frontend/collab (`domain`, `application`, `infrastructure`, `web`).
- Task 94: added refactor-budget guardrail script/config/override policy (`check_refactor_budget.py` + JSON configs).
- Task 100: added route-level DB access enforcement with baseline tracking (`check_route_db_access.py`, baseline snapshot).
- Task 109: added frontend feature dependency enforcement checks and feature-layer scaffolding.
- Wave F validation run:
- `docker compose run --rm backend pytest tests/test_documents.py` (pass)
- `docker compose run --rm backend ruff check app` (pass)
- `npm.cmd --prefix collab-server run typecheck` (pass)
- `npm.cmd --prefix collab-server run test` (pass)
- `npm.cmd --prefix frontend run build` (pass)
- `python scripts/architecture_checks/check_backend_architecture.py` (pass via Docker workspace run)
- `python scripts/architecture_checks/check_collab_architecture.py` (pass via Docker workspace run)
- `python scripts/architecture_checks/check_frontend_feature_dependencies.py` (pass via Docker workspace run)
- `python scripts/architecture_checks/check_route_db_access.py` (pass after baseline update; current=200, baseline=200)
- `python scripts/architecture_checks/check_refactor_budget.py` (script validated; backend Docker image lacked `git`, CI runner expected to execute this check)
- Completed Task 45: standardized backend core services around constructor-injected service objects.
- Refactored `AuthService`, `CommentService`, and `VersionService` to instance-first APIs and updated route/context call-sites to instantiate service objects per request.
- Reused shared service base types by introducing `SessionService` and wiring `DocumentService`/`AnalyticsService` to service-base inheritance for consistent structure.
- Task 45 validation run:
- `docker compose run --rm backend ruff check app/services/base_service.py app/services/auth_service.py app/services/comment_service.py app/services/version_service.py app/services/document_service.py app/api/management/auth.py app/api/management/comments.py app/api/management/versions.py app/application/contexts/reviews/api.py tests/test_comments.py` (pass)
- `docker compose run --rm backend pytest tests/test_auth.py tests/test_comments.py tests/test_versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_documents.py` (pass)
- Completed Task 50: introduced explicit access-policy classes (`DocumentAccessPolicy`, `ReviewPolicy`, `InvitationPolicy`) under `backend/app/application/policies/`.
- Centralized permission/tenant decisions through policy objects in `app/services/permissions.py` with compatibility wrappers for existing callers.
- Replaced ad-hoc review/invitation role+tenant checks with policy-based checks in `backend/app/api/management/reviews.py` and `backend/app/api/management/invitations.py`.
- Updated collaboration service/endpoints to use constructor-injected document access policy decisions while preserving existing route behavior (`backend/app/services/collaboration_service.py`, collaboration API routes, auth collab-token endpoint).
- Added policy unit coverage in `backend/tests/test_access_policies.py`.
- Task 50 validation run:
- `docker compose run --rm backend ruff check app/application/policies/__init__.py app/application/policies/access_policies.py app/services/permissions.py app/services/collaboration_service.py app/api/management/invitations.py app/api/management/reviews.py app/api/management/auth.py app/api/management/collaboration/state.py app/api/management/collaboration/snapshots.py app/api/management/collaboration/sessions.py app/api/management/collaboration/activity.py app/application/contexts/collaboration/api.py tests/test_access_policies.py` (pass)
- `docker compose run --rm backend pytest tests/test_access_policies.py tests/test_permissions.py tests/test_invitations_api.py tests/test_reviews_api.py tests/test_collaboration.py` (pass)
- Completed Task 51: added repository layer scaffolding under `backend/app/repositories/` for `Document`, `Version`, `User`, `Comment`, and `Invitation` aggregates.
- Migrated common aggregate fetch/query paths to repositories in auth, version, comment, invitation, and collaboration modules to reduce direct route/service ORM fetch duplication.
- Updated collaboration and invitation management endpoints to use repository-backed aggregate access patterns, including batched user lookups in list/feed/session responses.
- Task 51 validation run:
- `docker compose run --rm backend ruff check app/repositories/__init__.py app/repositories/base.py app/repositories/document_repository.py app/repositories/version_repository.py app/repositories/user_repository.py app/repositories/comment_repository.py app/repositories/invitation_repository.py app/services/auth_service.py app/services/version_service.py app/services/comment_service.py app/api/management/auth.py app/api/management/invitations.py app/api/management/collaboration/state.py app/api/management/collaboration/snapshots.py app/api/management/collaboration/sessions.py app/api/management/collaboration/activity.py` (pass)
- `docker compose run --rm backend pytest tests/test_auth.py tests/test_comments.py tests/test_versions.py tests/test_invitations_api.py tests/test_reviews_api.py tests/test_collaboration.py` (pass)
- Completed Task 52: introduced shared Unit-of-Work transaction helper at `backend/app/services/uow.py`.
- Migrated multi-step write flows in `DocumentService`, `VersionService`, and `CommentService` to explicit Unit-of-Work scopes for deterministic commit/rollback handling.
- Added Unit-of-Work tests in `backend/tests/test_uow.py` to validate auto-commit, rollback-on-exception, and explicit rollback behavior.
- Task 52 validation run:
- `docker compose run --rm backend ruff check app/services/uow.py app/services/document_service.py app/services/version_service.py app/services/comment_service.py tests/test_uow.py` (pass)
- `docker compose run --rm backend pytest tests/test_uow.py tests/test_documents.py tests/test_versions.py tests/test_comments.py` (pass)
- Completed Task 111: added explicit application use-case interfaces under `backend/app/application/interfaces/` for `PublishApprovedVersion` and `AssignCompanySet`.
- Added FastAPI dependency providers that resolve intent-specific use-case contracts (`get_publish_approved_version_use_case`, `get_assign_company_set_use_case`) and refactored management routes to call these interfaces instead of direct generic CRUD-style paths.
- Adapted existing services behind use-case contracts by adding `VersionService.publish_approved_version(...)` and `DocumentService.assign_company_set(...)`, and moved company-assignment validation/update logic into `DocumentService`.
- Added targeted interface coverage in `backend/tests/test_use_case_interfaces.py` (contract/provider checks + intent-alias delegation + assignment set semantics).
- Task 111 validation run:
- `docker compose run --rm backend ruff check app/application/interfaces app/services/document_service.py app/services/version_service.py app/api/management/documents.py app/api/management/versions.py tests/test_use_case_interfaces.py` (pass)
- `docker compose run --rm backend pytest tests/test_use_case_interfaces.py tests/test_versions.py tests/test_documents.py` (pass)
- Completed Task 74: introduced typed result algebra with `Result[T, E]` in `backend/app/domain/result.py`.
- Added app-layer command/query handlers using typed expected failures:
- `backend/app/application/commands/document_commands.py` (`AssignCompanySetCommandHandler`)
- `backend/app/application/commands/version_commands.py` (`PublishApprovedVersionCommandHandler`)
- `backend/app/application/queries/document_queries.py` (`GetDocumentQueryHandler`)
- Wired management routes to command/query handlers and explicit typed error-to-HTTP mapping for expected failures:
- `backend/app/api/management/documents.py` (`GET /documents/{id}/assigned-companies`, `POST /documents/{id}/assign-companies`)
- `backend/app/api/management/versions.py` (`POST /documents/{id}/versions/{id}/publish`)
- Added typed-result regression coverage in `backend/tests/test_result_algebra.py`.
- Task 74 validation run:
- `docker compose run --rm backend ruff check app/domain/result.py app/domain/__init__.py app/application/commands app/application/queries app/api/management/documents.py app/api/management/versions.py tests/test_result_algebra.py` (pass)
- `docker compose run --rm backend pytest tests/test_result_algebra.py tests/test_use_case_interfaces.py tests/test_versions.py tests/test_documents.py` (pass)
- Completed Task 64: introduced domain error hierarchy in `backend/app/errors.py` (`DomainError`, `NotFoundError`, `PermissionDeniedError`, `ConflictError`, `ValidationError`, `InvalidStateError`).
- Added centralized domain-error to HTTP mapping in `backend/app/main.py` via FastAPI exception handler (consistent status + `detail` + `error_code` payload).
- Migrated core document/version service paths away from direct `HTTPException` usage to domain errors (`backend/app/services/document_service.py`, `backend/app/services/version_service.py`).
- Updated Task 74 command adapters to classify expected failures from domain errors (while preserving legacy HTTPException compatibility fallback).
- Updated management routes to raise domain errors for typed command/query failures so transport mapping stays centralized.
- Added integration coverage for domain error mapping payloads in `backend/tests/test_domain_errors.py`.
- Task 64 validation run:
- `docker compose run --rm backend ruff check app/errors.py app/main.py app/services/document_service.py app/services/version_service.py app/application/commands/document_commands.py app/application/commands/version_commands.py app/api/management/documents.py app/api/management/versions.py tests/test_result_algebra.py tests/test_domain_errors.py` (pass)
- `docker compose run --rm backend pytest tests/test_domain_errors.py tests/test_result_algebra.py tests/test_use_case_interfaces.py tests/test_versions.py tests/test_documents.py` (pass)
- Completed Task 75: added backend composition root container and app factory (`backend/app/container.py`, `backend/app/app_factory.py`) and simplified `backend/app/main.py` to use `create_app()`.
- Routed service/use-case/command/query dependency providers through the shared container (`backend/app/dependencies/services.py`, `backend/app/application/interfaces/dependencies.py`, `backend/app/application/commands/dependencies.py`, `backend/app/application/queries/dependencies.py`), with direct-call fallback for test invocation.
- Updated management routes to consume container-backed dependencies (auth/comments/documents/versions) and aligned collab runtime bootstrap with explicit runtime dependency composition (`collab-server/src/index.ts`).
- Added container coverage in `backend/tests/test_container.py` and updated interface import paths to avoid package-level circular import coupling.
- Task 75 validation run:
- `docker compose run --rm backend ruff check app/application/interfaces/dependencies.py app/application/commands/dependencies.py app/application/queries/dependencies.py app/dependencies/services.py tests/test_use_case_interfaces.py tests/test_container.py` (pass)
- `docker compose run --rm backend pytest tests/test_container.py tests/test_use_case_interfaces.py tests/test_comments.py tests/test_documents.py` (pass)
- Completed Task 102: added centralized Policy Decision Point service with structured authorization decisions (`backend/app/policy/pdp.py`, `backend/app/policy/__init__.py`).
- Routed core permission decisions in service-layer helpers through the PDP while preserving existing public helper signatures (`backend/app/services/permissions.py`).
- Updated FastAPI permission dependencies to enforce route-level authorization through PDP-backed decision adapters (`backend/app/dependencies/permissions.py`).
- Added PDP-focused regression coverage for reason codes and role/document decision boundaries in `backend/tests/test_policy_pdp.py`.
- Task 102 validation run:
- `docker compose run --rm backend ruff check app/policy app/services/permissions.py app/dependencies/permissions.py tests/test_policy_pdp.py tests/test_permissions.py tests/test_dependencies_permissions.py` (pass)
- `docker compose run --rm backend pytest tests/test_policy_pdp.py tests/test_permissions.py tests/test_dependencies_permissions.py -q` (pass)
- Completed Task 114: added structured policy explanation objects with stable reason-code mapping and transport/debug helpers (`backend/app/policy/explanations.py`).
- Exported explanation API in policy package (`backend/app/policy/__init__.py`) and applied explanations to all permission dependency denials with consistent message format and `X-Policy-*` reason/action headers.
- Added explanation-focused regression tests for reason-code/message/header/log-context behavior and dependency integration (`backend/tests/test_policy_explanations.py`).
- Task 114 validation run:
- `docker compose run --rm backend ruff check app/policy app/dependencies/permissions.py app/services/permissions.py tests/test_policy_explanations.py tests/test_policy_pdp.py tests/test_permissions.py tests/test_dependencies_permissions.py` (pass)
- `docker compose run --rm backend pytest tests/test_policy_explanations.py tests/test_policy_pdp.py tests/test_permissions.py tests/test_dependencies_permissions.py -q` (pass)
- Completed Task 54: introduced domain value objects (`DocumentNumber`, `SemanticVersion`, `TopicSlug`) under `backend/app/domain/value_objects/` and exported them from the domain package.
- Replaced ad-hoc document numbering and semantic-version parse/format logic in `backend/app/services/document_service.py` and `backend/app/services/version_service.py` with value-object operations.
- Reworked topic normalization helpers to use `TopicSlug` canonicalization logic (`backend/app/utils/topic_normalization.py`) and aligned attachment semantic-version patch bump helper to `SemanticVersion` (`backend/app/services/attachment_service/common.py`).
- Added value-object regression coverage in `backend/tests/test_value_objects.py` and expanded version API regression to verify fallback semantic-version behavior on malformed prior values (`backend/tests/test_versions.py`).
- Task 54 validation run:
- `docker compose run --rm backend ruff check app/domain/value_objects app/domain/__init__.py app/utils/topic_normalization.py app/services/document_service.py app/services/version_service.py app/services/attachment_service/common.py tests/test_value_objects.py tests/test_versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_value_objects.py tests/test_documents.py tests/test_versions.py tests/test_public_api.py -q` (pass)
- `docker compose run --rm backend pytest tests/test_attachments.py -q` (pass)
- Completed Task 67: introduced aggregate roots under `backend/app/domain/aggregates/` (`DocumentAggregate`, `ReviewAggregate`, `InvitationAggregate`) and exported them in the domain package.
- Moved document lifecycle invariants and transitions into `DocumentAggregate` and consumed them from service/route orchestration (`backend/app/services/document_service.py`, `backend/app/services/version_service.py`, `backend/app/api/management/reviews.py`).
- Moved review workflow pending/version freshness/transition invariants into `ReviewAggregate` and invitation lifecycle invariants into `InvitationAggregate`, consumed by review and invitation management routes.
- Added aggregate-focused regression tests in `backend/tests/test_aggregates.py`.
- Task 67 validation run:
- `docker compose run --rm backend ruff check app/domain/aggregates app/domain/__init__.py app/services/document_service.py app/services/version_service.py app/api/management/reviews.py app/api/management/invitations.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_invitations_api.py tests/test_versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_aggregates.py tests/test_reviews_api.py tests/test_invitations_api.py tests/test_versions.py tests/test_documents.py -q` (pass)
- Completed Task 83: added domain factories for complex object initialization under `backend/app/domain/factories/` (`DocumentFactory`, `VersionFactory`, `InvitationFactory`) and exported them through `backend/app/domain/__init__.py`.
- Refactored document/version/invitation creation flows to use factory methods instead of repeated inline model construction (`backend/app/services/document_service.py`, `backend/app/services/version_service.py`, `backend/app/api/management/invitations.py`).
- Added factory-focused regression coverage in `backend/tests/test_domain_factories.py` and validated integration against document/version/review/invitation APIs.
- Task 83 validation run:
- `docker compose run --rm backend ruff check app/domain/factories app/domain/__init__.py app/services/document_service.py app/services/version_service.py app/api/management/invitations.py tests/test_domain_factories.py tests/test_documents.py tests/test_versions.py tests/test_invitations_api.py tests/test_reviews_api.py` (pass)
- `docker compose run --rm backend pytest tests/test_domain_factories.py tests/test_documents.py tests/test_versions.py tests/test_invitations_api.py tests/test_reviews_api.py -q` (pass)
- Completed Task 112: added reusable invariant specification package exports under `backend/app/domain/specifications/__init__.py` and surfaced specs through `backend/app/domain/__init__.py` for shared runtime/test usage.
- Refactored aggregate-root invariant enforcement to delegate to specification objects (`backend/app/domain/aggregates/document_aggregate.py`, `backend/app/domain/aggregates/review_aggregate.py`, `backend/app/domain/aggregates/invitation_aggregate.py`) while preserving existing error semantics/messages.
- Added specification-focused regression coverage in `backend/tests/test_invariant_specifications.py` to exercise satisfiability and assertion behavior across document/review/invitation invariants.
- Task 112 validation run:
- `docker compose run --rm backend ruff check app/domain/specifications app/domain/aggregates/document_aggregate.py app/domain/aggregates/review_aggregate.py app/domain/aggregates/invitation_aggregate.py app/domain/__init__.py tests/test_invariant_specifications.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_invitations_api.py tests/test_versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_invariant_specifications.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_invitations_api.py tests/test_versions.py -q` (pass)
- Completed Task 69: added reusable query/access specification objects under `backend/app/domain/specifications/queries.py` (`TenantScopeSpec`, `VisibilitySpec`, `RoleAccessSpec`, `DateRangeSpec`) and exported them through domain specification/domain package APIs.
- Composed query specs in repository/query-handler paths by extending `DocumentRepository` with spec-backed scoped query builders (`scoped_query_for_user`, `portal_visible_query_for_customer`) and wiring management search filters through tenant/date specs in both FTS and ORM fallback paths (`backend/app/api/management/search.py`).
- Replaced duplicated customer portal visibility/role filter logic with spec-driven gates in portal document routes (`backend/app/api/portal/documents.py`), including shared access checks for detail/attachment endpoints.
- Added query-spec regression coverage in `backend/tests/test_query_specifications.py` and validated search/portal behavior parity with existing endpoint suites.
- Task 69 validation run:
- `docker compose run --rm backend ruff check app/domain/specifications app/repositories/document_repository.py app/api/management/search.py app/api/portal/documents.py app/domain/__init__.py tests/test_query_specifications.py tests/test_search_api.py tests/test_portal_api.py` (pass)
- `docker compose run --rm backend pytest tests/test_query_specifications.py tests/test_search_api.py tests/test_portal_api.py tests/test_company_deactivation_access.py -q` (pass)
- Completed Task 84: introduced domain capability objects under `backend/app/domain/capabilities/` including explicit `CanPublish` and `CanAssignCompanies` permission capabilities plus role/document/review/user-management capability types.
- Refactored authorization evaluation flow in `backend/app/services/permissions.py` to resolve permissions into capability objects and attach capability metadata to PDP decisions while preserving existing reason-code/action semantics.
- Updated route/security consumers to execute capability-backed checks (`backend/app/dependencies/permissions.py` now evaluates resolved permission capabilities; `backend/app/security.py` tenant-active guard now relies on capability-backed role-membership evaluation).
- Added capability mapping/evaluation regression coverage in `backend/tests/test_capabilities.py`.
- Task 84 validation run:
- `docker compose run --rm backend ruff check app/domain/capabilities app/services/permissions.py app/dependencies/permissions.py app/security.py app/domain/__init__.py tests/test_capabilities.py tests/test_permissions.py tests/test_dependencies_permissions.py tests/test_policy_pdp.py tests/test_policy_explanations.py` (pass)
- `docker compose run --rm backend pytest tests/test_capabilities.py tests/test_permissions.py tests/test_dependencies_permissions.py tests/test_policy_pdp.py tests/test_policy_explanations.py tests/test_company_deactivation_access.py -q` (pass)
- Completed Task 87: enforced immutable command/query DTO boundaries in application handlers by adding immutable actor snapshots (`backend/app/application/dto.py`) and normalizing command payloads to immutable structures.
- Refactored command/query DTOs to immutable slot-based dataclasses and removed mutable sequence leakage (`backend/app/application/commands/document_commands.py`, `backend/app/application/commands/version_commands.py`, `backend/app/application/queries/document_queries.py`).
- Updated publish use-case interface typing to consume minimal user-actor shape for command boundaries (`backend/app/application/interfaces/use_cases.py`).
- Added regression coverage for DTO immutability and snapshot behavior in `backend/tests/test_immutable_dtos.py`.
- Task 87 validation run:
- `docker compose run --rm backend ruff check app/application/dto.py app/application/interfaces/use_cases.py app/application/commands/document_commands.py app/application/commands/version_commands.py app/application/queries/document_queries.py tests/test_immutable_dtos.py tests/test_result_algebra.py app/api/management/documents.py app/api/management/versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_immutable_dtos.py tests/test_result_algebra.py tests/test_use_case_interfaces.py tests/test_versions.py tests/test_documents.py -q` (pass)
- Completed Task 55: introduced explicit document/review workflow state-machine classes under `backend/app/domain/workflows/` (`DocumentWorkflow`, `ReviewWorkflow`) with centralized `can_transition` and `transition` APIs.
- Wired aggregate lifecycle transitions to workflow engines so status mutations flow through centralized transition rules (`backend/app/domain/aggregates/document_aggregate.py`, `backend/app/domain/aggregates/review_aggregate.py`), including document status normalization for new version candidates.
- Exported workflow classes through the domain package (`backend/app/domain/__init__.py`) for reuse by application/service layers and tests.
- Added workflow-focused regression coverage in `backend/tests/test_workflows.py` for valid transitions, invalid transition blocking, and document candidate-status normalization behavior.
- Task 55 validation run:
- `docker compose run --rm backend ruff check app/domain/workflows app/domain/aggregates/document_aggregate.py app/domain/aggregates/review_aggregate.py app/domain/__init__.py tests/test_workflows.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_versions.py` (pass)
- `docker compose run --rm backend pytest tests/test_workflows.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_versions.py tests/test_documents.py -q` (pass)
- Completed Task 88: introduced explicit workflow-stage objects under `backend/app/domain/states/` for document lifecycle states (`DocumentStage` variants) and review lifecycle/version-gating states (`ReviewStage` variants + `NoReviewStage`).
- Refactored `DocumentWorkflow` and `ReviewWorkflow` to delegate transition/normalization behavior to stage objects instead of enum transition tables, keeping existing transition semantics and error messages intact.
- Refactored version publish/update review-status guards in `backend/app/services/version_service.py` to use stage behavior (`version_review_stage_for(...).ensure_*`) rather than inline conditional chains.
- Exported stage objects through `backend/app/domain/states/__init__.py` and `backend/app/domain/__init__.py` for reuse by domain/application layers and tests.
- Added stage-focused regression coverage in `backend/tests/test_states.py` for valid/invalid transitions and publish/update guard behavior across `None`, `pending`, `approved`, `rejected`, and `cancelled` review stages.
- Task 88 validation run:
- `docker compose run --rm backend ruff check app/domain/states app/domain/workflows/document_workflow.py app/domain/workflows/review_workflow.py app/services/version_service.py app/domain/__init__.py tests/test_states.py` (pass)
- `docker compose run --rm backend pytest tests/test_states.py tests/test_workflows.py tests/test_aggregates.py tests/test_reviews_api.py tests/test_versions.py tests/test_domain_errors.py -q` (pass)
- Completed Task 70: introduced reusable command pipeline primitives under `backend/app/application/pipeline/` with explicit `validate -> authorize -> execute -> publish` stage contracts and per-run execution traces.
- Refactored existing write command handlers (`AssignCompanySetCommandHandler`, `PublishApprovedVersionCommandHandler`) to execute through the shared pipeline while preserving typed error mapping and endpoint behavior.
- Added review approval command orchestration (`backend/app/application/commands/review_commands.py`) and wired `POST /api/v1/reviews/{review_id}/approve` through the new handler so route logic is a thin command adapter (`backend/app/api/management/reviews.py`).
- Extended container/dependency wiring for the approve-review command handler (`backend/app/container.py`, `backend/app/application/commands/dependencies.py`) and exported new command types from command package API.
- Added regression coverage for stage ordering and handler trace behavior in `backend/tests/test_command_pipeline.py` and updated container dependency resolution coverage for the new handler in `backend/tests/test_container.py`.
- Task 70 validation run:
- `docker compose run --rm backend ruff check app/application/pipeline app/application/commands app/container.py app/api/management/reviews.py tests/test_command_pipeline.py tests/test_container.py` (pass)
- `docker compose run --rm backend pytest tests/test_command_pipeline.py tests/test_result_algebra.py tests/test_reviews_api.py tests/test_versions.py tests/test_domain_errors.py tests/test_container.py -q` (pass)
- Completed Task 56: expanded CQRS-lite handler coverage across core analytics and document management domains.
- Added analytics read-model query objects/handler facade under `backend/app/application/queries/analytics_queries.py` and wired analytics route endpoints (including CSV/PDF exports) through query handlers instead of direct service orchestration (`backend/app/api/management/analytics.py`).
- Added document-list query handler (`ListDocumentsQueryHandler`) and wired document list/read paths through query handlers (`backend/app/application/queries/document_queries.py`, `backend/app/api/management/documents.py`).
- Added document write command handlers for create/update/delete with typed error mapping and pipeline execution traces (`backend/app/application/commands/document_commands.py`), and wired document management write endpoints through command handlers (`backend/app/api/management/documents.py`).
- Extended container/dependency providers for new CQRS handlers (`backend/app/container.py`, `backend/app/application/commands/dependencies.py`, `backend/app/application/queries/dependencies.py`) and exported new handler/query types through package APIs.
- Added CQRS-lite unit coverage in `backend/tests/test_cqrs_handlers.py` and extended container dependency coverage for new handlers (`backend/tests/test_container.py`).
- Task 56 validation run:
- `docker compose run --rm backend ruff check app/application/queries app/application/commands app/api/management/analytics.py app/api/management/documents.py app/container.py tests/test_cqrs_handlers.py tests/test_container.py` (pass)
- `docker compose run --rm backend pytest tests/test_cqrs_handlers.py tests/test_command_pipeline.py tests/test_result_algebra.py tests/test_analytics.py tests/test_documents.py tests/test_domain_errors.py tests/test_container.py -q` (pass)
- Completed Task 71: moved high-complexity search and customer-portal reads into dedicated query handlers/read models.
- Added management search query handler module (`backend/app/application/queries/search_queries.py`) with explicit query/read-model DTOs for full-text search, autocomplete, facets, and saved-search listing.
- Added customer portal read-model query handler module (`backend/app/application/queries/portal_queries.py`) encapsulating portal document listing/detail, attachment metadata, categories, dashboard stats, and portal search snippet logic.
- Refactored management search and portal document routes to thin adapters that delegate to query handlers (`backend/app/api/management/search.py`, `backend/app/api/portal/documents.py`) while preserving response semantics.
- Extended query dependency/container wiring for search/portal handlers (`backend/app/application/queries/dependencies.py`, `backend/app/container.py`) and exported new query handler/DTO APIs (`backend/app/application/queries/__init__.py`).
- Added direct query-handler regression tests (`backend/tests/test_read_query_handlers.py`) and extended container dependency coverage for new handlers (`backend/tests/test_container.py`).
- Task 71 validation run:
- `docker compose run --rm backend ruff check app/application/queries app/api/management/search.py app/api/portal/documents.py app/container.py tests/test_read_query_handlers.py tests/test_search_api.py tests/test_portal_api.py tests/test_container.py` (pass)
- `docker compose run --rm backend pytest tests/test_read_query_handlers.py tests/test_search_api.py tests/test_portal_api.py tests/test_container.py -q` (pass)
- Completed Task 113: introduced reusable command/query bus middleware under `backend/app/application/bus/` with validation, authorization, and tracing middleware plus dispatch trace metadata.
- Wired container-backed command and query buses (`backend/app/container.py`) and routed dependency providers through bus adapters for command and query handlers (`backend/app/application/commands/dependencies.py`, `backend/app/application/queries/dependencies.py`).
- Added backward-compatible query adapter behavior so existing `execute_*` handler call-sites dispatch through the bus without route churn (`backend/app/application/bus/bus.py`).
- Added bus pipeline regression coverage in `backend/tests/test_bus_pipeline.py` for middleware ordering, authorization denial handling, adapter dispatch behavior, and trace capture.
- Task 113 validation run:
- `docker compose run --rm backend ruff check app/application/bus app/application/commands/dependencies.py app/application/queries/dependencies.py app/container.py tests/test_bus_pipeline.py` (pass)
- `docker compose run --rm backend pytest tests/test_bus_pipeline.py tests/test_command_pipeline.py tests/test_cqrs_handlers.py tests/test_read_query_handlers.py tests/test_container.py tests/test_versions.py tests/test_reviews_api.py tests/test_documents.py tests/test_search_api.py tests/test_portal_api.py -q` (pass)
- Completed Task 116: introduced reusable executable workflow model primitives under `backend/app/domain/workflows/models.py` to represent state graphs with reachability/path enumeration APIs.
- Wired lifecycle workflows to expose explicit executable models (`DocumentWorkflow.model()`, `ReviewWorkflow.model()`) while preserving existing transition error semantics in `backend/app/domain/workflows/document_workflow.py` and `backend/app/domain/workflows/review_workflow.py`.
- Exported workflow-model API through workflow/domain package boundaries (`backend/app/domain/workflows/__init__.py`, `backend/app/domain/__init__.py`).
- Added model-based transition coverage under `backend/tests/workflows/test_workflow_models.py` with exhaustive transition-matrix assertions, terminal/reachability checks, and deterministic lifecycle path checks for document/review workflows.
- Task 116 validation run:
- `docker compose run --rm backend ruff check app/domain/workflows app/domain/__init__.py tests/workflows/test_workflow_models.py tests/workflows/__init__.py tests/test_workflows.py tests/test_states.py` (pass)
- `docker compose run --rm backend pytest tests/workflows/test_workflow_models.py tests/test_workflows.py tests/test_states.py tests/test_versions.py tests/test_reviews_api.py -q` (pass)
- Completed Task 53: introduced domain event package under `backend/app/domain/events/` with immutable event DTOs (`DocumentPublished`, `CommentCreated`) and in-process dispatcher for synchronous handler execution.
- Added notification/email event handlers in `backend/app/services/domain_event_handlers.py` and dispatcher composition helper for service-layer usage.
- Refactored write flows to emit domain events instead of orchestrating email side effects inline:
- `VersionService.publish_version(...)` now dispatches `DocumentPublished`.
- `CommentService.create_comment(...)` now dispatches `CommentCreated`.
- Added regression coverage in `backend/tests/test_domain_events.py` for dispatcher fault-tolerance and event emission from version/comment services.
- Task 53 validation run:
- `docker compose run --rm backend ruff check app/domain/events app/domain/__init__.py app/services/domain_event_handlers.py app/services/version_service.py app/services/comment_service.py tests/test_domain_events.py` (pass)
- `docker compose run --rm backend pytest tests/test_domain_events.py tests/test_versions.py tests/test_comments.py -q` (pass)
- Completed Task 72: introduced durable outbox persistence model (`DomainEventOutbox`) and lightweight DB bootstrap migration for the outbox table/indexes (`backend/app/models/__init__.py`, `backend/app/db.py`).
- Added outbox dispatcher + serializer + worker processing loop in `backend/app/services/outbox.py`, including retry/backoff state transitions and event-key idempotent enqueue semantics.
- Added repository support for outbox claim/enqueue/runnable queries (`backend/app/repositories/outbox_repository.py`, `backend/app/repositories/__init__.py`).
- Added standalone worker entrypoint for outbox processing (`backend/app/workers/outbox_worker.py`).
- Wired core write flows to persist emitted events through outbox inside transaction boundaries (`backend/app/services/version_service.py`, `backend/app/services/comment_service.py`) while keeping side-effect handlers in `backend/app/services/domain_event_handlers.py`.
- Added outbox reliability regression coverage in `backend/tests/test_outbox.py` (idempotent enqueue, dispatch success marking, retry/failure transitions) and updated event shape coverage in `backend/tests/test_domain_events.py`.
- Task 72 validation run:
- `docker compose run --rm backend ruff check app/domain/events app/models/__init__.py app/repositories/__init__.py app/repositories/outbox_repository.py app/services/domain_event_handlers.py app/services/outbox.py app/services/version_service.py app/services/comment_service.py app/db.py app/workers/outbox_worker.py tests/test_domain_events.py tests/test_outbox.py alembic/versions/20260227_0004_domain_event_outbox.py` (pass)
- `docker compose run --rm backend pytest tests/test_outbox.py tests/test_domain_events.py tests/test_versions.py tests/test_comments.py -q` (pass)
- Completed Task 73: introduced explicit process-manager workflows under `backend/app/application/process_managers/` for upload/release-notes orchestration and conversion job execution (`DocumentUploadProcessManager`, `PreviewConversionProcessManager`).
- Refactored `/api/v1/documents/upload` to delegate multi-step create/attach/release-notes workflow and rollback compensation to `DocumentUploadProcessManager` (`backend/app/api/management/documents.py`) with deterministic execution/compensation traces.
- Refactored conversion-job execution to delegate stage flow + retry/failure compensation semantics to `PreviewConversionProcessManager` while preserving durable queue behavior (`backend/app/services/conversion_jobs.py`).
- Added process-manager coverage in `backend/tests/test_process_managers.py` plus integration validation against upload rollback and conversion queue suites.
- Task 73 validation run:
- `docker compose run --rm backend ruff check app/application/process_managers app/api/management/documents.py app/services/conversion_jobs.py tests/test_process_managers.py tests/test_upload_lifecycle_defaults.py tests/test_conversion_jobs.py` (pass)
- `docker compose run --rm backend pytest tests/test_process_managers.py tests/test_upload_lifecycle_defaults.py tests/test_conversion_jobs.py tests/test_documents.py -q` (pass)
- Completed Task 101: added durable idempotency-key persistence (`IdempotencyKeyRecord`) with schema bootstrap + managed Alembic migration (`backend/app/models/__init__.py`, `backend/app/db.py`, `backend/alembic/versions/20260227_0005_idempotency_keys.py`).
- Implemented `IdempotencyMiddleware` for replay-safe dedupe on selected side-effecting write endpoints (publish, invite/resend, upload, comment create, review submit/approve/reject/cancel) with payload fingerprint conflict detection and in-progress guarding (`backend/app/middleware/idempotency.py`).
- Wired idempotency middleware into app composition and middleware package exports (`backend/app/app_factory.py`, `backend/app/middleware/__init__.py`).
- Added idempotency regression coverage in `backend/tests/test_idempotency.py` for duplicate replay behavior, payload-mismatch conflicts, and single-execution side-effect guarantees.
- Task 101 validation run:
- `docker compose run --rm backend ruff check app/middleware/idempotency.py app/middleware/__init__.py app/app_factory.py app/models/__init__.py app/db.py tests/test_idempotency.py alembic/versions/20260227_0005_idempotency_keys.py` (pass)
- `docker compose run --rm backend pytest tests/test_idempotency.py tests/test_comments.py tests/test_versions.py tests/test_reviews_api.py tests/test_invitations_api.py -q` (pass)
- Completed Task 107: added optimistic-concurrency metadata on mutable resources with `row_version` + computed `etag` for `Document` and `Version`, including auto-increment on ORM updates (`backend/app/models/__init__.py`) and migration/bootstrap support (`backend/alembic/versions/20260227_0006_optimistic_concurrency_row_versions.py`, `backend/app/db.py`).
- Added reusable `If-Match` precondition enforcement helpers and explicit 428 precondition domain error (`backend/app/utils/concurrency.py`, `backend/app/errors.py`), then enforced checks in document/version update write paths (`backend/app/services/document_service.py`, `backend/app/services/version_service.py`, `backend/app/application/commands/document_commands.py`).
- Surfaced concurrency tokens through API contracts/responses and headers (`backend/app/schemas/__init__.py`, `backend/app/api/management/documents.py`, `backend/app/api/management/versions.py`) with ETag response headers on document/version reads and writes.
- Updated frontend API/types and document editing flows to send `If-Match` on mutable writes and preserve conflict visibility to users (`frontend/src/lib/api/documentsApi.ts`, `frontend/src/types/index.ts`, `frontend/src/pages/DocumentsPage.tsx`, `frontend/src/pages/document-detail/hooks/useContentEditingFlow.ts`, `frontend/src/pages/document-detail/hooks/useDocumentDetailPageState.ts`).
- Added optimistic-concurrency regression coverage in `backend/tests/test_optimistic_concurrency.py` and refreshed affected document/version/CQRS tests for precondition headers.
- Task 107 validation run:
- `docker compose run --rm backend ruff check app/errors.py app/utils/concurrency.py app/models/__init__.py app/db.py alembic/versions/20260227_0006_optimistic_concurrency_row_versions.py app/services/document_service.py app/services/version_service.py app/application/commands/document_commands.py app/api/management/documents.py app/api/management/versions.py app/schemas/__init__.py tests/test_optimistic_concurrency.py tests/test_documents.py tests/test_versions.py tests/test_cqrs_handlers.py` (pass)
- `docker compose run --rm backend pytest tests/test_optimistic_concurrency.py tests/test_documents.py tests/test_versions.py tests/test_cqrs_handlers.py tests/test_security.py -q` (pass)
- `cmd /c npm run build` (pass)
- Completed Task 106: introduced shared async-job framework primitives under `backend/app/jobs/` with unified retry policy/backoff/DLQ decisions (`RetryPolicy`, poison-message classification), standardized batch observability reports, and a shared polling worker runtime loop.
- Refactored durable outbox processing to use shared async-job policy/runtime semantics with explicit batch counters and structured retry vs DLQ transitions, while preserving compatibility wrappers (`backend/app/services/outbox.py`).
- Refactored durable conversion-job processing to use shared async-job policy/runtime semantics and normalized outcome reporting, while preserving existing enqueue/worker APIs (`backend/app/services/conversion_jobs.py`).
- Added operational DLQ tooling for visibility/recovery:
- Service-level list/requeue helpers for conversion and outbox dead-letter entries.
- New CLI admin tool `backend/app/workers/async_jobs_admin.py` for listing and requeueing failed jobs.
- Added async-job framework and recovery regression coverage (`backend/tests/test_jobs_framework.py`) plus DLQ list/requeue tests in conversion/outbox suites (`backend/tests/test_conversion_jobs.py`, `backend/tests/test_outbox.py`).
- Task 106 validation run:
- `docker compose run --rm backend ruff check app/jobs app/services/conversion_jobs.py app/services/outbox.py app/workers/async_jobs_admin.py tests/test_jobs_framework.py tests/test_conversion_jobs.py tests/test_outbox.py` (pass)
- `docker compose run --rm backend pytest tests/test_jobs_framework.py tests/test_conversion_jobs.py tests/test_outbox.py tests/test_process_managers.py tests/test_idempotency.py -q` (pass)
- Completed Task 105: introduced projection/cache runtime primitives under `backend/app/projections/` with scoped read-through caching, TTL-based reuse, payload-shape consistency validation, and deterministic scoped invalidation hooks.
- Added write-path invalidation baseline via SQLAlchemy session events (`after_flush` + `after_commit` + rollback cleanup) that map committed model mutations to read scopes (`analytics`, `search`, `portal`) and invalidate affected cached projections.
- Wired heavy read query handlers to projection cache wrappers with fallback behavior:
- `backend/app/application/queries/analytics_queries.py` now caches analytics overview/activity/engagement/top/users/content/feedback/tenant projections per tenant/system scope.
- `backend/app/application/queries/search_queries.py` now caches document search/autocomplete/facets projections per visibility scope and query inputs.
- `backend/app/application/queries/portal_queries.py` now caches customer portal list/detail/categories/dashboard/search projections per tenant/user scope.
- Registered projection invalidation bootstrap in app composition (`backend/app/app_factory.py`) and added test cache isolation fixture (`backend/tests/conftest.py`) to prevent cross-test state leakage.
- Added projection-specific regression coverage in `backend/tests/test_projection_cache.py` for cache-hit behavior, deterministic write-side invalidation, and explicit cache-path fallback handling.
- Task 105 validation run:
- `docker compose run --rm backend ruff check app/projections app/application/queries/analytics_queries.py app/application/queries/search_queries.py app/application/queries/portal_queries.py app/app_factory.py tests/conftest.py tests/test_projection_cache.py` (pass)
- `docker compose run --rm backend pytest tests/test_projection_cache.py tests/test_read_query_handlers.py tests/test_search_api.py tests/test_portal_api.py tests/test_analytics.py tests/test_container.py tests/test_cqrs_handlers.py -q` (pass)
- Completed Task 99: introduced a typed backend feature-flag framework (`backend/app/feature_flags.py`) backed by config env flags in `backend/app/config.py` for controlled architecture rollouts.
- Added backend rollout toggles:
- `FEATURE_FLAG_PROJECTION_CACHE` controls projection-cache execution in analytics/search/portal query handlers.
- `FEATURE_FLAG_IDEMPOTENCY_MIDDLEWARE` controls whether idempotency middleware is mounted in app startup.
- Wired app startup and read-path behavior to flags (`backend/app/app_factory.py`, `backend/app/application/queries/analytics_queries.py`, `backend/app/application/queries/search_queries.py`, `backend/app/application/queries/portal_queries.py`) so rollback can be done by config only.
- Added frontend feature-flag framework (`frontend/src/config/featureFlags.ts`) with safe boolean parsing and typed flag resolution.
- Wired frontend optimistic concurrency request headers behind `VITE_FF_OPTIMISTIC_CONCURRENCY_HEADERS` in `frontend/src/lib/api/documentsApi.ts`.
- Added rollout/rollback runbook documentation in `docs/feature-rollout-flags.md` and environment examples in `.env.example`.
- Added feature-toggle regression tests:
- Backend: `backend/tests/test_feature_flags.py`.
- Frontend: `frontend/src/config/featureFlags.test.ts`.
- Task 99 validation run:
- `docker compose run --rm backend ruff check app/feature_flags.py app/app_factory.py app/application/queries/analytics_queries.py app/application/queries/search_queries.py app/application/queries/portal_queries.py app/config.py tests/test_feature_flags.py tests/test_projection_cache.py` (pass)
- `docker compose run --rm backend pytest tests/test_feature_flags.py tests/test_projection_cache.py tests/test_analytics.py tests/test_container.py tests/test_idempotency.py -q` (pass)
- `cmd /c npm run test -- --run src/config/featureFlags.test.ts` (pass)
- `cmd /c npm run build` (pass)
- Completed Task 108: introduced explicit strangler wrapper boundaries under `backend/app/legacy_wrappers/` for legacy-heavy analytics and document-converter modules.
- Added wrapper usage + migration-status tracking (`backend/app/legacy_wrappers/tracking.py`) with per-wrapper metadata (`legacy_module`, `migration_completion_percent`) and call-volume counters.
- Added analytics wrapper boundary (`backend/app/legacy_wrappers/analytics.py`) and routed container analytics resolution through wrapper composition (`backend/app/container.py`) while preserving query-handler behavior.
- Added document-converter wrapper boundary (`backend/app/legacy_wrappers/document_converter.py`) and routed attachment conversion/read-path call sites through wrappers:
- `backend/app/services/attachment_service/artifacts.py`
- `backend/app/services/attachment_service/reader_view.py`
- `backend/app/services/attachment_service/upload.py`
- Added migration/runbook docs in `docs/legacy-strangler-wrappers.md`.
- Added strangler regression coverage in `backend/tests/test_legacy_wrappers.py` for:
- container wrapper resolution,
- call-volume tracking and migration metadata snapshots,
- wrapper delegation behavior,
- boundary enforcement that migration-target attachment modules no longer import converter utilities directly.
- Task 108 validation run:
- `docker compose run --rm backend ruff check app/legacy_wrappers app/container.py app/services/attachment_service/artifacts.py app/services/attachment_service/reader_view.py app/services/attachment_service/upload.py tests/test_legacy_wrappers.py tests/test_attachment_office_conversion.py tests/test_attachments.py tests/test_container.py` (pass)
- `docker compose run --rm backend pytest tests/test_legacy_wrappers.py tests/test_attachment_office_conversion.py tests/test_attachments.py tests/test_container.py tests/test_analytics.py -q` (pass)
- Completed Task 46: split oversized backend modules by converting `backend/app/services/analytics_service.py` into a facade over dashboard-section mixins in `backend/app/services/analytics/` and extracting DB migration logic out of `backend/app/db.py`.
- Task 46 converter split objective remains satisfied via the strategy/pipeline extraction completed in Task 60 (`backend/app/conversion/*` + legacy wrapper boundary).
- Added dedicated DB migration helper modules under `backend/app/infrastructure/db/` (`managed_migrations.py`, `lightweight_migrations.py`) and kept `app.db` compatibility wrappers (`_run_managed_migrations`, `_bootstrap_schema_from_metadata`, `_run_lightweight_migrations`) for existing runtime/tests.
- Updated managed-migration regression tests to track Alembic head dynamically and keep legacy schema setup compatible with current head revisions (`backend/tests/test_db_migrations.py`).
- Task 46 validation run:
- `docker compose run --rm backend ruff check app/services/analytics_service.py app/services/analytics app/db.py app/infrastructure/db tests/test_db_init.py tests/test_db_migrations.py tests/test_analytics.py` (pass)
- `docker compose run --rm backend pytest tests/test_db_init.py tests/test_db_migrations.py tests/test_analytics.py -q` (pass)
- Completed Task 85: introduced extension plugin registries under `backend/app/plugins/` for converter strategies, analytics exporters, and notification channels with lifecycle checks (register/load/validate/select).
- Replaced hardcoded selection paths with registry lookups:
- `DocumentConversionPipeline` now resolves converter plugins via `DocumentConverterPluginRegistry`.
- Analytics export endpoints now resolve format plugins via `AnalyticsExportPluginRegistry` (`csv`/`pdf`).
- Notification event handler composition now resolves channels through `NotificationChannelPluginRegistry`.
- Added plugin lifecycle regression coverage in `backend/tests/test_plugin_registries.py`.
- Completed Task 86: introduced anti-corruption adapters for collaboration contracts on backend and collab-server boundaries.
- Added backend collaboration ACL adapter package (`backend/app/adapters/collaboration/`) and wired `CollaborationService` token/permission payload normalization through `CollaborationContractAdapter`.
- Added collab-server contract adapters (`collab-server/src/adapters/collaborationTokenContractAdapter.ts`, `collab-server/src/adapters/documentStateContractAdapter.ts`) and wired auth/persistence flows through them (`authContext/collaborationAuthService.ts`, `persistence.ts`).
- Added adapter regression coverage in `backend/tests/test_collaboration_contract_adapter.py` and collab-server adapter tests (`collab-server/src/__tests__/collaborationTokenContractAdapter.test.ts`, `collab-server/src/__tests__/documentStateContractAdapter.test.ts`).
- Task 85/86 validation run:
- `docker compose run --rm backend ruff check app/plugins app/adapters app/conversion/document_pipeline.py app/api/management/analytics.py app/services/domain_event_handlers.py app/services/collaboration_service.py tests/test_plugin_registries.py tests/test_collaboration_contract_adapter.py` (pass)
- `docker compose run --rm backend pytest tests/test_plugin_registries.py tests/test_collaboration_contract_adapter.py tests/test_conversion_pipeline.py tests/test_notifications_pipeline.py tests/test_domain_events.py tests/test_analytics.py tests/test_collaboration.py -q` (pass)
- `npm.cmd --prefix collab-server run typecheck` (pass)
- `npm.cmd --prefix collab-server run test -- --runInBand src/__tests__/auth.test.ts src/__tests__/persistence.test.ts src/__tests__/collaborationTokenContractAdapter.test.ts src/__tests__/documentStateContractAdapter.test.ts` (pass)
- Completed Task 49: replaced deep nested API mixin composition with explicit module-list factory composition using typed helper (`frontend/src/lib/api/composition.ts`, `frontend/src/lib/api.ts`).
- Preserved existing API call surface (`api.<method>`) while making composition order declarative and easier to extend.
- Added file-level architecture note in `frontend/src/lib/api.ts` describing the composition intent.
- Started Task 63: introduced explicit API transport DTO contracts and mapper layer (`frontend/src/lib/api/dto/contracts.ts`, `frontend/src/lib/api/dto/mappers.ts`) and wired auth/users/documents API modules through mappers.
- Preserved existing API method signatures while centralizing transport-to-domain adaptation in one boundary layer (`frontend/src/lib/api/authApi.ts`, `frontend/src/lib/api/usersApi.ts`, `frontend/src/lib/api/documentsApi.ts`).
- Completed Task 63: expanded mapper coverage across all frontend API mixins under `frontend/src/lib/api/*.ts` (analytics, attachments, collaboration, companies, invitations, notifications, reviews, search/engagement) with explicit DTO contracts and mapping helpers.
- Added DTO mapper regression tests (`frontend/src/lib/api/dto/mappers.test.ts`) for nested response mapping and contract copy semantics.
- Completed Task 47: decomposed `DocumentsPage` into feature modules under `frontend/src/pages/documents/` with dedicated controller hooks (`useDocumentsPageController`, `useCreateDocumentFlow`, `useUploadDocumentFlow`) and extracted components for filters/table/quick-start/create/upload modals.
- Refactored `frontend/src/pages/DocumentsPage.tsx` into a composition-oriented page that delegates business flow logic to hooks and UI sections to feature components.
- Task 47 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- Completed Task 62: introduced frontend feature controller/view-model modules under `frontend/src/features/` for complex screens:
- `reviews`: `useReviewsPageController` hook + extracted tabs/filter/table components, with `frontend/src/pages/ReviewsPage.tsx` reduced to composition.
- `analyticsDashboard`: `useAnalyticsDashboardController` hook + access/tabs/section composition components, with `frontend/src/pages/AnalyticsDashboardPage.tsx` reduced to composition.
- Added feature-level utility regression tests for controller/view-model support logic:
- `frontend/src/features/reviews/constants.test.ts`
- `frontend/src/features/analyticsDashboard/constants.test.ts`
- Task 62 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run test -- src/features/reviews/constants.test.ts src/features/analyticsDashboard/constants.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Completed Task 76: introduced explicit frontend use-case service modules for complex document/review workflows under feature boundaries.
- Added `frontend/src/features/documents/useCases/documentsUseCases.ts` with reusable orchestration for document list query param building, draft creation + initial-version generation, upload metadata normalization, visibility updates, and upload file validation.
- Added `frontend/src/features/reviews/useCases/reviewsUseCases.ts` with reusable review approval/rejection/cancel command wrappers.
- Refactored page-level controllers/hooks to delegate business orchestration to use-case services:
- `frontend/src/pages/documents/hooks/useDocumentsPageController.ts`
- `frontend/src/pages/documents/hooks/useCreateDocumentFlow.ts`
- `frontend/src/pages/documents/hooks/useUploadDocumentFlow.ts`
- `frontend/src/features/reviews/hooks/useReviewsPageController.ts`
- Added use-case regression tests:
- `frontend/src/features/documents/useCases/documentsUseCases.test.ts`
- `frontend/src/features/reviews/useCases/reviewsUseCases.test.ts`
- Task 76 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run test -- src/features/documents/useCases/documentsUseCases.test.ts src/features/reviews/useCases/reviewsUseCases.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Completed Task 77: introduced explicit frontend workflow state machines for collaboration connection lifecycle and document content-editing flow.
- Added collaboration state machine module and transition tests:
- `frontend/src/features/collaboration/state/collaborationConnectionMachine.ts`
- `frontend/src/features/collaboration/state/collaborationConnectionMachine.test.ts`
- Added document-detail content-editing state machine module and transition tests:
- `frontend/src/features/documentDetail/state/contentEditingMachine.ts`
- `frontend/src/features/documentDetail/state/contentEditingMachine.test.ts`
- Added feature exports for machine boundaries:
- `frontend/src/features/collaboration/index.ts`
- `frontend/src/features/documentDetail/index.ts`
- Refactored hook orchestration to dispatch explicit machine transitions:
- `frontend/src/lib/useCollaboration.ts` now drives connect/sync/disconnect/reconnect/online/offline through `collaborationConnectionMachine`.
- `frontend/src/pages/document-detail/hooks/useContentEditingFlow.ts` now drives chooser/editing/token-handling through `contentEditingMachine`.
- Task 77 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run test -- src/features/collaboration/state/collaborationConnectionMachine.test.ts src/features/documentDetail/state/contentEditingMachine.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Completed Task 78: added contract-first OpenAPI generation flow for frontend transport DTOs and integrated drift checks into frontend build/test scripts.
- Added backend OpenAPI snapshot source:
- `backend/openapi.contract.json`
- Added generation tooling:
- `scripts/api_contracts/refresh_openapi_snapshot.mjs` (refreshes snapshot from running backend OpenAPI URL)
- `scripts/api_contracts/generate_frontend_contracts.mjs` (generates typed frontend contracts from snapshot with `--check` mode)
- Added generated contract output consumed by frontend API boundary:
- `frontend/src/lib/api/generated/openapi-contracts.ts`
- Updated frontend scripts to integrate contract checks into build/test flow:
- `frontend/package.json`
- `build` now runs `generate:api-contracts:check` before compile.
- `test` now runs `generate:api-contracts:check` before vitest.
- Added explicit contract generation commands:
- `generate:api-contracts`
- `generate:api-contracts:check`
- `refresh:api-contracts`
- Updated DTO transport contract layer to consume generated OpenAPI types for core resources while preserving mapper/domain compatibility:
- `frontend/src/lib/api/dto/contracts.ts`
- Task 78 validation run:
- `npm.cmd --prefix frontend run generate:api-contracts` (pass)
- `npm.cmd --prefix frontend run generate:api-contracts:check` (pass)
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run test -- src/lib/api/dto/mappers.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Task 63 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run test -- src/lib/api/dto/mappers.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Task 49 validation run:
- `npm.cmd exec -- tsc --noEmit -p tsconfig.json` (pass, in `frontend/`)
- `npm.cmd --prefix frontend run build` (fails in sandbox: esbuild `spawn EPERM`)
- `npm.cmd run test -- src/lib/api/httpClient.test.ts --run` (fails in sandbox: esbuild `spawn EPERM`)
- Completed Task 110: introduced initial Backend-for-Frontend layer for document detail orchestration.
- Added backend BFF endpoint and payload contract:
- `GET /api/v1/bff/documents/{document_id}/detail-page`
- `backend/app/api/bff/documents.py`
- `backend/app/schemas/__init__.py` (`DocumentDetailPageBundleResponse`)
- `backend/app/web/router_registry.py` router registration.
- Added backend regression tests for composed payload and tenant-scoped access:
- `backend/tests/test_bff_documents_api.py`.
- Added frontend BFF client boundary + query:
- `frontend/src/lib/api/bffApi.ts`
- `frontend/src/lib/api.ts`
- `frontend/src/lib/api/dto/contracts.ts`
- `frontend/src/lib/api/dto/mappers.ts`
- `frontend/src/hooks/useDocumentQueries.ts`
- `frontend/src/lib/queryKeys.ts`
- Refactored document detail page state to consume one bundled query instead of orchestrating 4 separate calls:
- `frontend/src/pages/document-detail/hooks/useDocumentDetailPageState.ts`
- Updated hook tests for new bundle query invalidation semantics:
- `frontend/src/pages/document-detail/hooks/useDocumentDetailPageState.test.tsx`
- `frontend/src/types/index.ts`.
- Task 110 validation run:
- `cmd /c npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` (pass)
- `cmd /c npm --prefix frontend run test -- useDocumentDetailPageState.test.tsx` (fails in sandbox: esbuild `spawn EPERM`)
- `python -m pytest backend/tests/test_bff_documents_api.py` (cannot run in this environment: `python.exe` inaccessible)
- Completed Task 117: introduced consumer-driven contract testing baseline with explicit, versioned consumer contracts and backend provider verification.
- Added frontend consumer contract definition + fixtures:
- `frontend/src/test/contracts/backendProvider.contract.json`
- `frontend/src/test/contracts/backendProvider.contract.test.ts`.
- Added backend provider verification suite that loads consumer contracts and verifies OpenAPI operation/response compatibility plus collaboration token payload claims:
- `backend/tests/contracts/test_provider_consumer_contracts.py`
- `backend/tests/contracts/__init__.py`.
- Wired explicit provider verification stage into backend CI job:
- `.github/workflows/ci.yml` (`backend-tests` now runs `pytest tests/contracts -v` before full backend suite).
- Completed Task 79: added cross-service contract checks for backend/frontend/collab integration assumptions.
- Added collab-server consumer contract definition + token fixture and adapter compatibility checks:
- `collab-server/src/__tests__/contracts/backendProvider.contract.json`
- `collab-server/src/__tests__/contracts/backendProvider.contract.test.ts`.
- Task 117/79 validation run:
- `cmd /c npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json` (pass)
- `cmd /c npm --prefix collab-server run typecheck` (pass)
- `cmd /c npm --prefix frontend run test -- src/test/contracts/backendProvider.contract.test.ts` (fails in sandbox: esbuild `spawn EPERM`)
- `cmd /c npm --prefix collab-server run test -- --runInBand src/__tests__/contracts/backendProvider.contract.test.ts` (pass)
- `python -m pytest backend/tests/contracts/test_provider_consumer_contracts.py` (cannot run in this environment: `python.exe` inaccessible)
- `docker compose run --rm backend pytest tests/contracts/test_provider_consumer_contracts.py -q` (cannot run in this environment: Docker pipe access denied)
- Completed Wave L (frontend architecture and contract alignment): 49, 63, 47, 62, 76, 77, 78, 110, 117, 79.

2026-02-25:
- Cleaned this plan file to open work only.
- Added Tasks 41-49 from latest clarity/OOP review.
- Added Tasks 50-65 with additional OOP opportunities across backend, frontend, and collab-server.
- Added Tasks 66-80 (additional design/OOP architecture options) with priorities and waves.
- Added Tasks 81-90 (additional OOP/design architecture options) with priorities and waves.
- Added Tasks 91-92 (ADR workflow and deprecation lifecycle governance).
- Added Tasks 93-100 (remaining architecture governance and operational hardening ideas).
- Added Tasks 101-122, plus ROI ranking and validation/smoke coverage updates.
- Reordered Tasks 101-122 into dependency-first logical sequencing.
- Reordered all execution waves (D-O) to strict logic/dependency sequencing across Tasks 41-122.
- No new tasks completed yet.
